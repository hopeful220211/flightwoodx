# 产品行为埋点

> 状态：实现与本地验收完成；未发布、未开启正式采集
>
> 更新时间：2026-09-14
>
> 适用范围：官网来源、创作、保存、预览反馈、编程、模拟、传播与导出
>
> 替代关系：落实用户确认的埋点方案，不替代现有业务数据、隐私规则或发布授权。

## 范围与当前证据

- 用户已确认第一方、必要事件、前后端分工、去重、测试隔离和隐私选择。
- 基线 `7a46441`；工作树 `/Users/nesty/Projects/flightwoodx-analytics`，分支 `codex/product-analytics-2026-09-14`。保留 animation/review/原仓库所有无关改动，不在本轮整合或发布。
- 已有累计时长接口没有前端调用方；新建空草稿、2秒自动保存与重复发布均不能直接计有效作品。
- 业务记录仍由 DroneDesign/Program/CustomPart/CommunityPost 负责。分析记录只解释过程。
- 不接第三方、录屏、指纹或任意文字采集。不扩展课程管理和监护人验证；未满14岁不启用可选行为统计。本人满14岁声明不是监护人验证。

## 步骤与所有权

1. 主代理：共享运行时契约、事件白名单与测试；先锁定 API 再并行。
2. API：独立受控收集接口、明确同意凭据、撤回删除、身份隔离、90天原始记录与去重；服务器确认成功事件；管理员汇总与CSV。
3. Web 客户端：默认关闭/拒绝不采集、可访问同意选择、设置与撤回、有限队列、同事件重试、身份切换清理。
4. Web 产品：真实操作完成点，预览状态转换，模拟运行结果，传播/导出准确命名。
5. 主代理整合：端到端与三尺寸浏览器验证、全量 CI、文档与交付。

## 验收

- 先观察新增测试失败，再实现。共享 schema 拒绝未知属性/个人信息、服务器事件伪造和过量批次。
- API 用隔离本地 Mongo 验证唯一索引、拒绝未同意/过期/跨账号/已撤回、重试、管理员权限、业务成功记录与错误不阻塞。
- 浏览器在390×844、768×1024、1440×900验证拒绝、同意、修改/保存、刷新、撤回、换账号；核对实际接收数量而非仅监测调用。
- 重复保存不成为新增作品，程序写入不等于已绑定，复制链接不等于传播，下载触发不等于磁盘写入。
- 完整 `pnpm run harness` 和 `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci`；测试账号/环境不混入正式统计。

## 发布与恢复

- 本轮不提交、推送、部署或改正式数据库。未来须先更新兼容 API/新集合与索引，再更新 Web，合成数据检查后开启配置。
- `ANALYTICS_ENABLED` 默认关闭；关闭开关可停止采集。恢复代码不删除现有业务数据；撤回仅删除对应分析记录，不删除作品。
- 新增统计起始时间与口径版本，不补造历史月活。复杂留存/归因看板后续建设；第一版保证记录可核对。

## 实际结果

- 20类前端事件、6类服务端成功事件接入；共享白名单、可选同意、签名凭据、有限队列、业务成功钩子、唯一索引、90天TTL、撤回删除及管理员汇总/CSV完成。未新增第三方依赖，未更改既有作品数据结构。
- 共享契约、错误边界、客户端授权/身份、服务端持久化及产品hooks按红→绿验证。独立复核修正了旧作品首次观察误计编辑、索引未就绪时并发重复、部分批次失败的撤回竞态、joint/deco类别错配、快速登录队列丢失、延迟授权漏记当前作品、前后端作品ID无法关联等问题。
- 完整 `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 通过843项；最后补双ID与白名单测试后，同一CI组成命令 `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run check` 和 `pnpm run build` 再次全通过：最终845项（根123、共享包104、Web512、API106），API无跳过。harness、类型、语法、lint、密钥扫描、依赖审计、构建通过。保留既有JointDirectionMark热更新警告和Blockly大包提示。
- 日志：`/tmp/fwx-analytics-ci-final.log`、`/tmp/fwx-analytics-check-complete.log`、`/tmp/fwx-analytics-build-complete.log`。最终构建入口 `index-BeFwFROb.js`，本地静态预览4182连接隔离API3002；未改用户原4173预览。
- 实际Chrome三尺寸390×844、768×1024、1440×900均通过：同意前零发送，拒绝后可绘制；同意后产生形状、预览阻塞/恢复和来源记录，每尺寸实际Mongo读回5条且事件ID唯一；撤回后对应记录为0，刷新不再采集。没有水平溢出或页面异常；主代理查看了设置、二维画板和真实木纹预览截图。
- 真实注册/保存浏览器检查通过：快速注册的auth_started与服务端auth_completed各1条，旧游客仍为guest，新授权记录为account；空草稿与非空保存分开，重载/自动保存后非空有效保存仍为1，重新打开作品有记录，作品名称和完整查询参数未进入事件，撤回后账号授权记录为0。最终核对服务器ID和localDesignId对应实际保存作品。
- 最终静态构建的既有核心浏览器回归4/4通过（1.2分钟），涵盖桌面跨会话恢复、手机拼装/编程/模拟、全部官方模型资源、真实自制件保存及失败恢复。开发服务器首轮桌面模拟在编辑热更新期间被重置，静态构建重测通过，未以增加等待时间或改模拟逻辑掩盖失败。日志 `/tmp/fwx-analytics-core-browser-final.log`。
- 专项日志：`/tmp/fwx-analytics-browser-check-final.log`、`/tmp/fwx-analytics-auth-check-final.log`、`/tmp/fwx-analytics-auth-check-reference-final.log`；截图 `/tmp/fwx-analytics-settings-{390,768,1440}.png`、`/tmp/fwx-analytics-editor-{390,768,1440}.png`。测试使用本地专用库 `fwx_analytics_browser_20260914` / `fwx_analytics_disabled_browser_20260914`，不含真实用户数据。
- Browser plugin not available；按前端测试技能的允许回退使用仓库Playwright和实际Chrome。分析技能将采集限制为固定事件/必要属性，并明确完成条件、身份隔离和指标口径；没有全站自动抓取或录屏。

## 尚未包含

- 未提交、推送、部署或开启正式采集，也未整合其他工作树的首页/发布流程改动。
- 未建设图形看板、历史留存快照、跨游客/账号逐人归因或未满14周岁的监护人授权；原始数据90天后过期，不能据此重建更早月活。
- 当前单API实例摘要去重与有界队列为尽力记录，多实例上线前需补跨实例原子去重。指标只代表已同意参与者，不代表全平台全部用户。
- 事件、指标、接口、配置和后续开启步骤见[使用统计规格](../../product-specs/analytics.md)。
