# 2026-09-15 合并发布

状态：已上线；2026-09-20正式前端切换及线上验收完成。

更新时间：2026-09-20。

适用范围：本轮已获授权的产品、兼容API及发布工作流更新。

替代关系：完成后更新正式版本证据；此前单项执行记录保留为历史。

## 范围

- 用户要求首页三架无人机按前后层次上下漂浮并轻微倾斜，以及发布此前全部已完成更新。
- 合并 `flightwoodx-dji-design` 的视觉、注册、两种拼装模式、自制零件连接和本轮悬停；合并 `flightwoodx-release` 的发布提速规则、单构建及四组浏览器检查。
- 原始工作树保留；生产基线 `71697fc6a670b25f7d2a7cc36b03aa7b7cba7e0e`，API `flightwoodx-api:4b05017`。兼容API先于前端更新，无数据库迁移，不改变现有密钥、存储和权限。

## 验收与进度

- 首页近/中/远浮动幅度11/7/4px、倾角1/0.7/0.4度，周期6.4/8.2/10.6秒且相位错开；系统减少动画时静止，离屏和后台暂停。
- 新增三尺寸浏览器回归；先确认旧版缺失动效而失败。初次实现发现两侧图旋转越过手机/平板边缘，增加6px内缩后复验。
- 修正主图居中变量被基础图片规则覆盖；三尺寸悬停回归3项通过（5.2秒），截图已检查。发布程序目标80项通过。
- 最终候选云端完整CI一次，复用同一前端产物；生产只验收本轮路径，不重复完整流程。
- 2026-09-15 16:52开始；旧SSH身份文件缺失，现有阿里云终端也已断开，用户重新连接后继续已有管理通道，不新增授权。

## 回退

- API更新前保存数据库、上传卷与当前容器配置。前端使用受限发布器保留旧版本。
- 新自制连接数据写入后不能降回不认识该契约的API；若前端回退，保留兼容API。
- 实际提交、测试、服务器备份、切换和耗时在完成后补入本记录，不以候选上传代表上线。

## 实际发布证据

- 最终候选 `bc30b5c4547ede081ccfaf0f1b4e6937f1dd20bb`；API镜像 `flightwoodx-api:a4e27af`，与最终候选的API及共享运行源码无差异，后续三次提交仅修正测试脚本。
- 17:19:15兼容API切换完成，公网健康接口200、数据库connected。配置环境变量集合及上传挂载未变，MongoDB与nginx容器ID未变。最初严格比较环境变量数组因顺序变化触发自动回退，确认键值完全一致后切换成功。
- 私有服务器备份目录为本轮终端的 `$FWX_DEPLOY_DIR`（`/root/fwx-combined-20260915-*` 独立目录）；包含停止API写入后备份的数据库和上传卷、原/新配置、切换前后容器记录。压缩包校验通过。无数据库迁移。
- API服务器构建90.4秒。云端候选CI `34951384185`；已通过全部非浏览器检查及浏览器1、3、privacy组。第2组因刷新前GET响应被浏览器销毁而失败，原提交仅重试失败组，不重跑成功组、不重建产物。
- 第2组重试仍出现同一旧响应问题；改为同账号新页面读取并检查真实保存数据，本地目标用例通过4.9秒。最终完整CI `34952492486` 全部成功；提交和8个必需检查经发布证据程序核对通过，产物 `10389777988`，摘要 `sha256:648fb86316678f9c2c3173c979a88ef8febcdc57ff76f7e97754b34ea917be0b`。
- 正式分支已普通快进至最终提交；生产任务 `34953433967` 复用上述同一产物，不重新构建或重跑业务测试。
- 上述生产任务实际跳过上传，正式前端仍为 `71697fc6`，不得视为已上线。原因是复用时浏览器分组被跳过，传递依赖使上传作业默认被跳过。9月20日恢复时原产物已过期，必须重新取得归档。
- 本轮仅修复上传作业条件：显式状态判断，同时要求四个直接前置检查全部成功、生产分支push且未取消；不放宽任何检查或生产权限。新增回归先失败，再确认17项发布客户端测试通过。产品与API源码不再修改。
- 正式前端 `c785b94dd3040f198b73ab1f1cf2307770b5f210`。云端完整检查 `35500416845` 全部通过；测试归档 `10601997159`，ZIP摘要 `sha256:2810095fad38872bdc7876748c4dcb61bb265c5f8a7f04ed80c933e320c7a35e`。
- 生产任务 `35500803234` 实际上传成功，16:53:33开始、16:55:21结束（108秒）；上传作业66秒。复用上述归档，无生产重建。公网release.json为正式提交，首页200/no-cache，入口JS200/immutable，API数据库connected；发布器通过文件摘要和公网验证。
- 正式站390×844、768×1024、1440×900三尺寸：三图动画实际变换、减少动画时静止、注册勾选默认关闭与按钮禁用、说明分行、无横向溢出通过。游客新建作品的两种模式均可见且无默认选择，未选择时不能继续。操作前正常拒绝可选统计通知；未新增正式账号或作品。截图 `/tmp/fwx-live-20260920-vXy4LB`。
- 9月20日从16:42开始核对至16:58收尾，约16分钟；原产物过期及发布条件修复需一次新云端检查。本轮没有失败后自动重跑、重建API或数据库操作。9月15日的前置开发、重试和中断不计入本轮，保留上述历史记录。
