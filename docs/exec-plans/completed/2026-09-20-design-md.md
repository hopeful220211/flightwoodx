# awesome-design-md 全站细节应用

状态：本地实现和验收完成，未发布
更新时间：2026-09-20
适用范围：全部Web路由、共享组件、编辑器和后台
替代关系：扩展已确认DJI视觉细节；不替代业务规则或发布授权

## 范围与来源

隔离分支 `codex/design-md-polish-2026-09-20`，基线 `34c6887`。固定上游 `8147538b4226ae41e2487a9179e3bcc1f68e8554`，保留MIT、README和Apple设计分析原文；项目根DESIGN.md按其九部分组织，本站已确认规则优先。没有执行上游代码，没有新增依赖。

内容保持检查覆盖所有247份既有非测试TS/TSX：忽略className/style后比较语法树，其他文字、链接、事件、图片路径、DOM顺序和几何属性必须相同；public目录不得变更。字体、主题色、首页大品牌字、图片与画布布局保留。

## 页面覆盖

| 页面族 | 路由与状态 | 处理 |
| --- | --- | --- |
| 展示 | 首页、关于/联系 | 已确认大标题/图片/营销按钮保留；全站焦点和辅助文字规则 |
| 账号 | 注册、登录跳转/弹窗、游客、个人页、资料 | 表单高度/字体/焦点、小圆角、标题/提示节奏 |
| 法务 | 隐私目录、五份政策、隐私设置、条款、未知政策 | 统一页面标题和边距，正文15/32保留 |
| 作品 | 工作台、作品列表/详情、命名/改名/发布/设置/删除弹窗 | 共享控件、对齐、长名称、数字、模态边距 |
| 社区 | 社区列表/详情、榜单、作者、收藏列表/详情、关注动态、评论/快览 | 收敛过大标题、超宽字距、9–11px文字；表单/空态/失败/分页 |
| 绘制 | part-studio、属性、插接口/放置/连接、零件列表 | 辅助文字12px、统一字段；位置、工具、几何和材质不动 |
| 编辑 | 自由/步骤拼装、各步骤/零件栏、编程/积木预览、仿真 | 顶栏点击区、字体/标签；第三方绘图坐标与功能色保留 |
| 运行/导出 | fly、AR、导出预览、报告/下载/缺数据 | 共享文字/控件规范；原数据显示/规则不动 |
| 后台 | 角色拒绝、密码入口、概览、用户、课程/零件占位、审计 | 小圆角导航、普通页面标题、表单/表格、三尺寸边距 |
| 公共 | 404、页面加载、错误边界、提示/Toast、隐私弹层 | 字体继承、按钮/焦点、辅助字号；原提示保持 |

## 验证

- 设计回归最初3项失败（小字/字距、缺统一字段类、缺细节契约），实现后通过；扫描和根设计文件检查放在根测试，遵守Web源码读取边界。
- 内容保持检查：最终247份通过、0项内容或静态资源变化；`git diff --check`通过。
- 完整CI通过994项（根216、共享109、Web562、API107），检查54.4秒、Web构建9.62秒。仅保留既有热更新和大包提示，无已知依赖漏洞；日志 `/tmp/fwx-design-md-ci-final.log`。
- 首次CI只完成类型检查，因新增测试跨目录读取规范被边界检查拦截；将该测试移到根脚本后通过。未绕过检查，也没有业务源码变更。
- 开发预览19项浏览器检查通过：三尺寸视觉/注册/作品控件，选中布局不跳动、8个缩放锚点、触控撤销/取消；日志 `/tmp/fwx-design-md-first-browser.log`。最终构建验证复用未受后续测试文件调整影响的证据。
- 路由矩阵准备阶段修正了三个测试问题：编程画布是SVG而非canvas；API通配拦截误拦开发源码模块；旧项目详情使用服务端Project id而不是设计id。没有为测试修改产品逻辑。
- 同一最终构建11项浏览器测试全部通过，2.9分钟：真实注册、两种拼装、自制连接、保存/跨会话恢复、编程、仿真、资源可用性5项；全部页面族在390×844、768×1024、1440×900下的6个路由/后台矩阵。日志 `/tmp/fwx-design-md-final-browser.log`。
- 矩阵产出135份页面/弹窗截图，无页面横向溢出、错误边界或页面异常；检查实际字体和字段几何。已人工查看三尺寸的注册、社区详情、项目设置、后台、绘图和自由拼装代表截图，目录 `/tmp/fwx-design-md-captures/`。社区/后台用隔离浏览器读数据夹具，社区缺模型/图片状态为刻意测试；账号/设计/连接/保存使用真实本地API。这不代替真实设备或生产后台权限验收。
- 原有生产包体提示保留，首页入口409.35KB（gzip126.97KB）；没有引入设计项目运行时依赖。预览 `http://127.0.0.1:4192/` 使用最终构建；API和浏览器写入仅在独立本地测试库。
- 不发布、不改API/数据库契约、依赖或生产配置；原任务工作树不动。回退仅撤销本隔离分支的样式/规范改动，无数据迁移。
