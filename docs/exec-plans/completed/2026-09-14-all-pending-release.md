# 未发布功能正式发布

> 状态：前后端已正式发布，公网及三尺寸验收通过
>
> 更新时间：2026-09-14
>
> 适用范围：本任务已完成的首页、文案、备案、媒体、关于/联系、作品控件和零件绘制前后端修改
>
> 替代关系：执行用户本轮全量发布授权；不覆盖各功能历史验证记录或硬件/制造边界

## 范围与基线

- 工作树为 `flightwoodx-review`，开发分支 `codex/home-hero-honors-2026-09-13`，开始时 HEAD `8187f07`，同一任务全部 WIP 保留。
- 公网 `release.json`、远端生产分支和受限 SSH 状态均读回 `e9f32aac00340eca4834586d1acc382613a7eb61`，API 健康且数据库已连接；服务器 `ready=true`。
- 包含此前全部已完成的功能、源文件、资产与测试。关于页只发布现有保守介绍，不猜测公司成立日期或两校共建关系；原五条评价已有用户公开授权。
- 不增加产品功能、不改变角色/公开范围、不删除生产记录、不重签证书、不升级数据库或放宽发布权限。

## 顺序与验收

1. 核对未发布清单、敏感文件、兼容性和现有生产访问方式。
2. 完整本机 CI、真实浏览器验证，提交完整候选并检查确切 SHA 的远端八项 CI。
3. 保存 API 镜像/配置、数据库和上传备份；只读检查存量自制零件的新契约兼容性。先升级 API 及共享运行时，保留 Mongo、上传卷、JWT 与证书。
4. 在后端确认兼容后，普通快进受保护的 `production`，由既有工作流发布同一浏览器测试产物。
5. 公网读回版本、关键文件摘要、三种尺寸界面及独立私有验收账号保存/重登录恢复，记录实际结果。

## 停止与回滚

- 专用 SSH 只接受前端 status/publish/rollback，不能用来绕过权限升级 API。没有合法后端发布通道时暂停整体切换，并请求必要登录。
- 开始时 GitHub CLI 与 Git 推送凭据均返回认证失败；用户已完成标准设备登录，`gh api user`确认为原账号，远端读取恢复。旧通用 root SSH 配置指向的本机私钥不存在，不能据此认为具备服务器 shell。已请用户登录阿里云；浏览器控制接口当前无法正常读取控制台。
- 新 mainboard 数据产生后，旧前端/后端枚举不能消费它；不能直接回退整套旧应用，更不能恢复旧数据库覆盖新作品。保留新契约的兼容候选，出错优先修复或以兼容版本回退。
- 任何备份、容器身份、挂载、健康或产物验证失败时不切换；仅本地测试通过不表示上线。

## 结果

### 最终发布结果

- 2026-09-14 17:30:07（Asia/Shanghai），[正式发布流程](https://github.com/hopeful220211/flightwoodx/actions/runs/34827331919)全部成功，包括8项检查和 `Publish tested frontend`。该流程72项浏览器测试通过（7.2分钟），发布其自身测试后的同一归档，artifact ID `10340867229`，ZIP摘要 `08c94aee95f6989365ba00def08f1df97be303150dc6555a21d23ee47655f9fc`。
- [正式站](https://flightwoodx.com/) `release.json`读回 `c7b69d7be0409f8229523fec5f4a8fde1003c0cd`、903项公开文件。受限发布状态 `ready=true`，当前c7b69d7、前一版本e9f32aa。首页HTML、入口JS/CSS、零件绘制JS、关于页JS及木纹纹理6项线上字节长度与SHA-256均符合清单；健康接口 `OK / connected`。
- 正式浏览器390×844、768×1024、1440×900各12组，共36组全部通过。覆盖实际11.5秒循环视频解码播放、五条原评价、备案图标/链接、关于/联系/合作入口、作品圆角、稳定选择与锚点、2mm木纹、板外孔不清空预览、普通孔缩放、两类插槽锁宽/方向、简短错误说明及修正恢复。无控制台错误/警告、失败网络或HTTP错误；这些游客界面检查无正式写请求。独立正式API保存/权限/重新登录验收见下文11步结果，不用游客界面检查替代它。
- 主代理与独立代理检查了三尺寸实际绘图、木纹与错误提示截图。只读报告及截图位于 `/tmp/fwx-live-all-20260914-zyf88Y/results/`；API无敏感信息报告 `/tmp/fwx-production-smoke-znWQkz/report.md`。含测试凭据的私有文件不得提交或公开。
- 全部本轮已完成功能已上线。公司成立日期及学校合作表述等此前未完成的内容不据此补写；2条旧契约标记和1条旧无效几何仍为原有数据问题，未冒充修复。备份与原镜像保留；未扩大权限、改变证书、迁移数据库或覆盖用户数据。

以下记录保留执行过程中的当时状态；最终上线结果以上述核验为准。

### 2026-09-14 接续全部更新发布

- 用户再次明确要求全部更新上线。目标仍为同一任务工作树 `flightwoodx-review`，开发分支 `codex/home-hero-honors-2026-09-13`，开始 HEAD 为 `ea1f8ca`；包含其后85项文件变更（62项已有文件修改、23个新增文件），不仅发布旧候选。
- 当日公网 `release.json` 与远端 `production` 均为 `e9f32aac00340eca4834586d1acc382613a7eb61`，健康接口返回 `OK / connected`。GitHub认证有效；前一候选 `ea1f8ca` 的远端8项CI已确认全部通过，但不替代新候选检查。
- 当前源码已完成748项本地自动测试和72项真实Chrome浏览器测试，原始日志分别为 `/tmp/fwx-concise-copy-ci.log` 与 `/tmp/fwx-concise-all-browser.log`。独立只读复核确认全部目标功能及资产在树中，源媒体与构建副本摘要一致。此次提交需包含新增 `packages/geometry/tsup.config.ts` 与全部新组件/测试。
- 后端审查确认：新 `mainboard` 与 `jointGuides` 必须先由API支持；无必需数据迁移、环境或索引修改。正式旧 `customparts` 仍须按候选几何规则只读核对。API仅替换自身，保留Mongo、上传卷、JWT、网络和当前nginx挂载；然后沿用原前端发布器，无需放宽权限。
- 原阿里云控制台已登录，已通过现有ECS页面进入Workbench，使用原先已启用的免密连接；未新建或扩大权限。当前仍在发布准备，尚未切换正式服务。

### 2026-09-14 后端切换与网页发布运行

- 全部候选提交为 `c7b69d7be0409f8229523fec5f4a8fde1003c0cd`，已推送开发分支；[候选CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34825771428)8项全部通过，包含72项真实浏览器测试（7.9分钟）和API容器测试。候选前端归档208,399,304字节，GitHub artifact ID `10340622907`，归档ZIP摘要 `2e33c6159fd2c7b5cf3fa67b2dd15976a5596a8ebe8125d9f79149891d77205a`；此开发分支产物未直接发布。
- ECS从确切提交建立独立 `/root/flightwoodx-api-c7b69d7` 工作树，构建 `flightwoodx-api:c7b69d7` 成功。原 `f5b12b3` 镜像与旧工作树保留；未更新Mongo、证书、系统权限或部署器。
- 私有备份 `/root/fwx-release-20260914-aFndSk`（0700）保存原镜像、容器配置、证书/部署配置、上传和数据库。压缩包完整性检查通过；数据库在无网络的临时Mongo中恢复成功，全部集合数量与正式库相同。切换前短暂停API，另外保存最终数据库与上传备份，不覆盖初始备份。
- 存量自制件只读审查：3条记录中2条 `manufacturability.passed` 不符合原契约；旧、新规则结果一致。按相同ID顺序，原API镜像与新镜像的几何结果均为 `[true,false,true]`，即1条已有无效轮廓，没有本轮新增不兼容。未修写历史记录或将其标为有效；这些遗留记录不属于本轮已修复范围。
- 09:18 UTC仅重建API，新镜像healthy；新旧完整环境变量（含JWT）和上传挂载一致，Mongo/Nginx容器身份保持。最终停写快照与启动后全部数据库文档SHA-256一致；nginx配置检查与重新载入成功，公网 `/api/health` 返回 `OK / connected`。
- 正式API隔离实测11步通过：新主机身及两类插槽、旧PUT省略标注保留、无效修改不覆写、私有作品保存与重复保存、重新登录恢复，以及未登录/跨账号访问拒绝。仅创建2个默认学生测试账号和本轮私有作品/零件；后两者按ID、名称及owner核对后清理，回读404。2个专用测试账号保留（无本人注销接口），凭据仅存本机0600测试文件，未触碰已有用户数据。
- 已普通快进 `production` 从 `e9f32aa` 至 `c7b69d7`；[正式发布流程](https://github.com/hopeful220211/flightwoodx/actions/runs/34827331919)运行中。网页仍须等发布步骤成功、公网版本与实际界面验收后才确认上线。

### 新增待发布修改

在`ea1f8ca`推送后，用户继续要求修复板外孔槽清空预览并重排绘制布局。新增本地改动见[绘制空间记录](../completed/2026-09-14-part-studio-workspace.md)，已通过完整CI653项与真实Chrome全站56项测试。因此下面`ea1f8ca`及其远端CI仅代表前一候选，不能直接作为本轮最新发布结果。后续应以包含新增修改的确切提交发布；合法后端管理会话要求不变。

本次重新运行完整 `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 成功：634项测试、API无跳过，类型、lint、harness、密钥扫描、安全审计和构建通过；日志 `/tmp/fwx-all-release-ci.log`。入口仍为 `index-DDUCIilk.js`。真实Chrome、隔离本地API/Mongo上的全套浏览器回归49/49通过（3.1分钟），包括390×844、768×1024、1440×900与触控操作；日志 `/tmp/fwx-all-release-browser.log`。补充文档后harness及diff检查再次通过。

已将全部139项文件改动提交为 `ea1f8ca6ec72e79548c861bffa362bc7a25e4d93`，推送原开发分支；[确切提交的远端CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34777408713)已排队，尚未得到结果。未推进`production`、未部署。前后端均需要此次发布；没有数据库迁移、新索引或新环境变量。等待用户登录阿里云以取得合法后台更新会话，后续必须先核验存量自制件和备份，不能只推静态站。
