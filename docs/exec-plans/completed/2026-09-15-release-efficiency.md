# 发布流程精简与并行检查

状态：本地实施及验收完成；未推送，新工作流尚未在云端启用，正式网站未切换。

更新时间：2026-09-15。

适用范围：发布入口、CI构建/浏览器分组、证据校验及每轮固定规则。

替代关系：更新9月14日发布优化；保留版本绑定、完整测试、权限与回退约束。

## 目标与范围

- 普通发布先检查历史/差异；复用同一提交证据，避免等待中反复查询全部证明及输出长日志。
- 一次前端构建，普通浏览器用例分三组，隐私用例单独隔离；全部成功后才提升归档为可发布产物。
- 开发迭代只运行相关检查；最终本地完整检查和云端完整检查按用途二选一，不在上传阶段重复开发验收。云端八项保护检查保持。
- 线上按改动范围验收，不重复三尺寸全部业务流程；给出时间目标、软告警、停止等待及失败处理规则。
- 不改产品、业务数据、生产保护或服务器权限。API镜像检查仍保留；后端无变化时不部署后端。后续如精简后端CI，须有依赖范围证据，不能仅看网页文件名跳过。

## 验证与交付

先为单次构建、完整分组证据、缺组拒绝、摘要和预检/等待行为编写失败测试，再实现。运行目标测试、完整本地CI；真实隔离浏览器检查使用同一归档，核对所有用例恰好覆盖一次。云端运行器速度与实际下一轮发布耗时须上线启用后实测，不用本地数据代替。

规则由根AGENTS、项目Skill和固定发布程序入口连接；记录仅保留一份，状态页及索引链接本记录。工作树复用干净的flightwoodx-release，新分支codex/release-efficiency-2026-09-15；原工作树不变。

## 回退

尚无生产修改。撤回本轮工作流/客户端及规则即可恢复上一发布流程；旧证据兼容读取，不覆盖正式归档，不为报告再次部署。

## 已取得的本地证据

- 测试先行：记录默认等待、预检、重复证明查询、单次构建、分组证明、归档解包的失败结果，再实现。改名后的 `release:check` 触发旧策略断言失败，按新入口语义修正；未放宽测试或隐藏失败。
- `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 通过：966 项（根 210、共享 105、Web 545、API 106）；API 未跳过。harness、类型/API语法、lint、安全和最终构建通过。最终检查阶段 45.8 秒，构建另计；仅为本地数据。
- Playwright 原清单 92 项；分组为 33 / 27 / 26 / 6，集合一致，重复为零。保持 `fullyParallel:false`、`workers:1`、`retries:0`。分组依据采用 [Playwright 官方分组说明](https://playwright.dev/docs/test-sharding)，并非删掉用例。
- 同一测试产物 SHA、摘要、每个文件完整性、拒绝覆盖旧目录，以及缺组/错 SHA/失败/跳过真实步骤等均有回归测试。源码分支和生产分支保护所需的八个作业名称不变。
- 真实本地 Chrome：同一归档分别解包至四个新目录；独立 API 3391–3394、Web 5191–5194、Mongo 临时库及上传目录。33 / 27 / 26 / 6 项全通过，零跳过、零重试、零失败。四组分别约 210.0 / 107.7 / 90.6 / 59.7 秒，并行阶段总计 **215.4 秒**；结束后关闭这些服务、清除本次临时库和目录，不触及既有测试服务。
- 真实 `pnpm release` 只读计划 **3.963 秒**，正确报告未提交变更及缺少该提交 CI 证据，不推送；回执写入 `.git/fwx-releases/`，stdout 不再重复完整差异和阶段历史。
- 最后补强了精简输出仍须保留 `ready` 和阻断原因的测试，再运行最终完整 CI，通过全部 966 项及构建。最终文档改动只补查 harness 与差异空白检查，不为报告再次跑完整业务测试。
- GitHub 分组运行器实际并行时间、跨运行器归档传递与下一轮正式发布总时长仍须授权启用后测量；**215.4 秒是本地浏览器分组验收，不是上线时长**。下一轮发布报告应分别列核对、候选 CI、生产发布、线上操作验收及总用时，再与规则目标比较。
