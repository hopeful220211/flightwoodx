# 两项任务合并发布

状态：已完成合并、前后端发布和正式网站验收。前端23:09:30上线，三尺寸公网浏览器检查23:16:33通过（北京时间）。

更新时间：2026-09-14。

适用范围：本任务与“全面审查项目现状 (2)”已完成的代码，以及同一首页工作副本的后续标题修改。

替代关系：本记录完成后更新当前正式版本；此前各功能记录的“未发布”保留为当轮历史状态。

## 范围与原始工作副本

- 插接口、曲边槽底保存、固定2mm槽宽、绘制后回到选择工具、工具名称提示、边缘高亮和操作指引。
- 首页动图无暂停按钮、获奖横条及英文标题 Montserrat Black。
- 第一方可选使用统计、授权和撤回、管理员汇总/CSV、隐私目录、五份政策和底部提示。正式采集仅在明确同意且确认年满14周岁后启用，管理员和既有专用测试账号排除。
- 固定发布入口、分阶段计时、独立检查并行、同一完整提交的可信CI产物复用，以及直线历史提前检查。
- 未添加课程、订购或支付等尚属讨论的产品功能；未替换中文字体。

四个工作副本分别为 `flightwoodx-insert-interface`、`flightwoodx-analytics`、`flightwoodx-animation`、`flightwoodx-review`。使用临时索引制作独立快照，不改变原索引、文件和未提交修改；最终再次逐一比较快照树，四处均无遗漏或变化。原主工作副本的无关修改未纳入。

快照分支为 `codex/release-snapshot-{insert-interface,analytics,animation,review}-2026-09-14`；中间合并分支 `codex/combined-release-2026-09-14` 保留。最终发布分支为 `codex/combined-release-linear-2026-09-14`。

## 提交与发布顺序

- 原正式前端/API：`c7b69d7be0409f8229523fec5f4a8fde1003c0cd`。
- 中间应用版本：`4b05017a33d8ffb50300ca19d606b16253a74ca4`，八项云端检查通过。
- 最终候选：`71697fc6a670b25f7d2a7cc36b03aa7b7cba7e0e`，单父提交，直接以原正式提交为父。
- 最终候选相对4b05017仅增加发布程序的合并提交检测、对应测试和固定规则；`apps/` 与 `packages/` 无差异。已经更新的API无需再次重建和重启。
- 先部署兼容API；前端仅在最终候选的全部八项CI和产物核验通过后，普通快进推送到 `production`。不强推、不修改分支保护、不扩大服务器权限。
- 这是后端及发布程序首次合并启用，按独立流程执行。`pnpm release` 会正确列出混合范围，不能绕过其前端单独发布限制。最新只读回执记录 `mergeCommits: []`。

## 后端证据

- 2026-09-14 22:45:55（北京时间）更新API，备份和切换约4秒。运行镜像 `flightwoodx-api:4b05017`，容器健康；服务器检出的完整应用版本与上面中间提交一致。
- 新备份目录 `/root/fwx-combined-20260914-GgwkLA`，目录权限700。保留切换前数据库归档、上传文件归档、原容器信息及原配置，归档gzip校验通过。恢复方法与当日上一轮已经完成的隔离恢复演练相同；本次不迁移业务集合。
- 切换前停止API，保存一致数据库和上传文件备份；只更新API服务。原登录及业务环境变量逐项相等，上传卷完全相同，MongoDB和Nginx容器ID不变，数据库未重建。Nginx配置检查通过并重新加载。
- 最终启用配置为备份目录内 `compose-candidate.json`；`compose-rollback.json` 保留原镜像和环境。统计签名密钥在服务器独立生成，未输出、上传到源码或写入报告。
- 已创建三个统计集合的唯一/查询/到期索引。事件和授权按到期字段清理；管理员及两名既有发布测试用户不纳入统计。
- 公网真实验证：健康与数据库连接、统计配置、无授权请求拒绝、专用测试账号排除、曲边插接口保存及重新读取、跨账号拒绝访问全部通过。仅使用既有专用测试账号，临时私有零件验证后已删除，没有创建正式账号。
- 证据文件：`/tmp/fwx-live-combined-20260914-uvrEec/api-result.json`；结果时间22:46:26。

## 检查与异常

1. 合并后的完整本地CI：958项自动测试通过；并行检查阶段51.6秒，随后构建成功。
2. 本地浏览器：隐私6项、插接口/首页等21项通过；修正旧工具总数断言后相关6项通过。额外三尺寸只读流程通过，覆盖动图实际播放、隐私政策、工具提示、插接口、自动回选择和木板预览；没有页面错误或写入请求。
3. [首次云端检查](https://github.com/hopeful220211/flightwoodx/actions/runs/34855862694)：83项浏览器用例通过、3项失败。原因是旧测试仍期望7个绘图工具，新增插接口后实际8个。修正总数并增加插接口可见性断言，未修改产品或放宽操作检查。
4. [第二次云端检查](https://github.com/hopeful220211/flightwoodx/actions/runs/34857375330)：八项检查通过；86项普通浏览器用例和6项真实隐私用例通过。普通流程使用关闭统计的隔离API，隐私流程重启为启用统计的隔离API，使用同一前端构建。
5. 第二候选普通推送被生产分支的直线历史规则拒绝，正式前端未改变。该限制未在最初核验，是额外耗时原因。随后保留原分支，整理单父发布提交，并补入程序提前拦截和固定规则；27项发布程序及规则测试通过。
6. [最终候选检查](https://github.com/hopeful220211/flightwoodx/actions/runs/34858816222)：八项全部成功；959项自动测试通过（根203、共享105、Web545、API106），86项普通浏览器用例和6项真实隐私用例通过。API镜像检查成功。

## 前端发布及公网验收

- [生产流程34860152242](https://github.com/hopeful220211/flightwoodx/actions/runs/34860152242)成功，正式版本为 `71697fc6a670b25f7d2a7cc36b03aa7b7cba7e0e`。可信源检查为34858816222，实际 `reuse=true`，未重复安装项目依赖、构建或执行全套浏览器测试。
- 产物ID `10354807231`，名称 `web-release-71697fc6a670b25f7d2a7cc36b03aa7b7cba7e0e`；208441169字节。ZIP摘要 `a3fabdf803450cbada3077a4a7ffabad9230f816163fd889dea01fad9a528bf4`，内部发布归档摘要 `6ee954a8cb9ca0fa28c53ec8dcbc90826b43e31222d7ba89bc5efa42908dbf5b`；下载后实际核验通过。
- 发布器读回当前71697fc、前一c7b69d7、`ready=true`。独立公网验证再次确认完整版本、入口文件字节摘要及API健康。
- `/`、`/index.html`、`/privacy/policy` 返回200及 `no-cache`；带摘要的入口脚本返回200及 `max-age=604800, public, immutable`。不将此结论扩展到未检查的资源。
- 正式Chrome的390×844、768×1024、1440×900流程全部通过：首页动图实际播放且无暂停按钮，获奖横幅、五份政策、拒绝统计、游客绘图、工具名称、曲边插接口、槽底/方向、固定2mm槽宽、自动回选择、实际木纹预览及无横向溢出。没有页面错误或写入请求；本次是浏览器尺寸模拟，不是真实平板硬件测试。
- 首次公网检查在木纹下载完成前启动5秒渲染断言而失败；改为等待实际纹理响应完成，再执行原5秒断言，三个尺寸通过。纹理实际下载约6秒，未修改产品、模拟资源或放宽渲染断言。
- 结果 `/tmp/fwx-live-combined-20260914-uvrEec/browser-result.json`；正式截图 `studio-390.png`、`studio-768.png`、`studio-1440.png`、`home-390.png`、`home-768.png`、`home-1440.png` 位于同目录。主代理已查看桌面/手机绘制及平板首页截图，确认木纹、槽口、工具与横幅实际显示。

## 耗时记录

用户本轮请求开始：22:20:20。第一轮云端检查22:28:43至22:40:22；第二轮22:42:36至22:53:16。最终候选22:55:52启动，约23:07:31完成。

生产流程23:08:02至23:09:33，共91秒；其中产物下载及硬校验7秒，实际上传、切换及公网核对23:08:51至23:09:30，共39秒。请求至页面上线约49分10秒，至最终公网浏览器通过约56分14秒，不把39秒说成整轮耗时。

本轮额外等待来自旧测试修正和未提前核验直线历史，不是上传速度。后续必须先核验提交历史，再启动候选CI；同一提交通过后只发布已测归档，不重复生产构建和浏览器回归。本轮已实测生产复用成功；正常候选仍需完整检查，不能承诺未来每轮固定用时。

## 回退与完成条件

- 前端切换失败使用现有发布器保留的旧版本，不删除旧归档。
- 新前端启用后若需要回退页面，应保留当前兼容API，确保已有插接口数据及隐私撤回能力仍可用。旧API配置只用于本轮切换失败的即时恢复，不能在新数据出现后直接降级。
- 完成条件均已满足：最终可信CI及摘要通过、生产发布成功、正式版本和入口文件摘要相符、公网三尺寸本次操作通过、实际截图检查完成。发布证据文档单独归档，不为文档记录再次部署网站。
