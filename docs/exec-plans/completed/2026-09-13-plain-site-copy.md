# 全站功能说明改写

> 状态：本地修改与验收完成，未发布
>
> 更新时间：2026-09-13
>
> 适用范围：首页顶部之外的前端静态文案、对应回归与本地浏览器验收
>
> 替代关系：按用户最新要求扩大此前三栏文案修改范围；不覆盖当前功能/部署事实或用户创建的内容

> 后续确认：2026-09-13 用户已确认原五条评价真实收集且可公开，评价板块已恢复；本文将评价改为说明的决定已被替代，当前结果见 `CURRENT_STATUS.md` 的原用户评价恢复记录。其他文案改动保持。

## 目标与边界

- 用简单陈述说明页面用途、可执行操作和当前状态，删除广告式、拟人化和无依据承诺。
- 冻结 `Home/sections/HeroSection.tsx` 与 `hero/`，保留此前已确认的标题、介绍、荣誉和统计行移除结果；本次不改其字节。
- 审核首页其余部分、账号、作品、社区、后台、设计/绘图/编程/模拟/导出、公共布局及可见静态数据。清楚的按钮、单位、错误和真实用户内容不机械替换。
- 不增加业务功能、修改权限/数据规则、重新设计布局或编造联系方式及教学/硬件事实，不提交、推送或发布。仅修正文字导致或验收暴露的局部换行问题。历史文档和不被页面使用的教学草稿不作为当前功能宣传。

## 当前依据

- 基线 `8187f07`，工作树 `/Users/nesty/Projects/flightwoodx-review`，分支 `codex/home-hero-honors-2026-09-13`；5项已存在修改均为本任务之前同一首页修改链，保留。
- 根/模块规则、`ARCHITECTURE.md`、`CURRENT_STATUS.md`、`docs/product-specs/core-flow.md` 与实际路由/数据为能力依据。
- 三个角色卡片目前通向同一登录弹窗，不称独立学校管理入口；课程/零件审核未开放；视觉仿真不等于实机；二维制造来源不全。
- 用户明确要求陈述而非促销。文案Skills中的情绪增强、转化口号或无证据量化不采用。

## 分工与步骤

1. 主代理：冻结首屏摘要、盘点范围、首页/布局文案、回归规范与整合。
2. 工具页代理：设计、绘图、Blockly、模拟与导出文案，相关局部测试。
3. 账号社区代理：注册登录、作品、社区、个人中心与后台，相关局部测试。
4. 只读审查代理：跨页面遗漏、角色入口、假统计/引言和能力边界核验。
5. 合并检查：一致用词、未完成标记、无旧口号、保留原功能，目标测试和完整CI。
6. 本地正式构建实际浏览器：390×844、768×1024、1440×900的首页和代表页面，核心E2E/图片/视频/登录与保存回归；使用隔离测试数据库，不接触正式账号。
7. 记录覆盖范围、截图/命令、未验证项，完成后移入completed并更新状态。

## 验收与停止条件

- 首页Hero及其荣誉/无人机子组件摘要保持，其他改动均能对应功能说明、状态准确性或检查。
- 先失败后通过的关键文案回归、`pnpm run harness` 和带真实本地MongoDB的 `pnpm run ci` 通过；相关浏览器回归通过，不放宽断言掩盖失败。
- 当时的轮播改写保留交互，以操作说明替代人物评价；此项已由后续用户确认与原评价恢复替代。
- 如需改变业务逻辑才能兑现文案，保留当前行为并明确限制，不扩展实现范围；新发现功能问题记录而非藏入文字修改。
- 撤回方式为恢复本轮文字及测试；保留之前未提交改动，不重置工作树。

## 结果

### 已完成改写

- 首页改为平台功能、设计工作台、使用步骤、学生/教师/学校、操作说明及创建设计作品；三栏保留自主设计/木质拼接/飞行测试的明确顺序。三个身份入口说明为共用登录，不再把登录按钮称为联系我们。
- 当时将人物评价替换为五条操作说明，保留轮播、前后切换与自动播放；后续已据用户确认恢复原评价。流程链接定位至真实使用步骤的修正继续保留。
- 账号、作品管理、社区/作者/收藏/关注/点赞榜、个人资料、后台占位、零件绘制、拼装、积木编程、模拟、AR、导出和公共页脚均按实际能力改写；共享校验提示保留原数量、阈值、规则、协议与标识。
- 明确课程/学校管理/实机控制尚未开放；本机学习记录不与账号同步；自制零件只能未连接摆放；当前网页导出入口只提供设计记录和零件清单、不提供切割图；撤下无依据的切割长度、板材数量和时间演示数值。
- `apps/web/AGENTS.md` 补充持久文案规则：普通陈述、操作与入口一致、必要限制说明、无编造证言/奖项/未来承诺、保留用户内容及锁定区域。

### 验收发现

- 390px个人页既有资料头部让15字符英文昵称将编辑按钮推至约427px。真实截图与尺寸断言先失败；仅该页补文字容器最小宽度和换行、头像与按钮不收缩，不截断用户内容。新增组件回归和手机核心E2E覆盖，修后真实浏览器通过。
- 零件工坊居中的绝对定位工具条默认收缩至半个画布宽，“预览3D”及撤销/清空出现竖向换行；390/768px浏览器断言先失败，按钮高76px。只修工具条尺寸与按钮单行约束，不改绘制/保存逻辑。
- 首次全套浏览器运行恰遇并行构建更新预览静态文件，最后一个桌面登录用例进入应用错误页，其余15项通过。停止重建后相同首页三视口连续三轮9/9通过；最终验收必须使用稳定构建，不能在浏览器运行时重新构建。
- Browser plugin not available，按前端验收Skill使用仓库既有Playwright与本机Chrome；独立检查脚本在`/tmp`，账号/密码仅测试进程内使用。所有写入指向本地独立MongoDB，不读写正式账号。

### 最终验证

- `pnpm run harness` 与 `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 全部通过，505项自动测试（根123、共享包62、Web240、API80）、API无跳过、类型检查/静态检查/构建通过，依赖审计未发现已知漏洞。保留既有Blockly 706.52kB构建提示，没有放宽门禁。
- 最终稳定构建 `http://127.0.0.1:4173/`，入口`index-ebIDre8_.js`，19/19 Playwright回归通过（1.6分钟）：桌面与390px注册、拼装、保存、跨设备恢复、积木编程和模拟；自制零件绘制、未连接摆放、恢复与失效引用；官方模型/缩略图；首页荣誉、视频原位播放/错误重试/键盘；三个身份共用登录、五条操作说明和使用步骤定位；三尺寸工具条按钮单行且不越界。
- 9个代表页面×390×844、768×1024、1440×900，共27次真实Chrome页面检查通过：注册、作品、个人资料、社区、点赞榜、收藏、关注、零件绘制、实机控制说明。测试账号实际登录，检查页面身份、非空、无框架错误页、无横向溢出或相关控制台/网络错误；已查看首页、注册、作品、社区、个人资料和绘图截图，资料编辑与工具条显示恢复正常。
- 多轮隔离注册曾触发本地认证20次/15分钟的正常429限制。重新启动隔离预览服务后，最终27次检查通过；未修改限流配置或任何正式服务。最终预览API只监听127.0.0.1:3000，使用独立测试库`fwx_plain_copy_ui_final`，与生产账号隔离。
- 独立只读审查通过。Hero三文件SHA-256与本轮开始一致：`HeroSection.tsx`为`a13aa024a42b0a6b2d8f6f3c723508eeb42321cb6a6fc8641f9ce5df8714a412`；`hero/HeroDrone3D.tsx`为`97028de717db3e7d643ac68ddf7c06c98bf55b531decbb7263ec53835a5862e0`；`hero/HeroHonors.tsx`为`56e9625f949a6c2c3bc919e0a2ba0184bc2feb7848041da438b0a64878e559da`。
- 证据：`/tmp/fwx-plain-copy-final-ci.log`、`/tmp/fwx-plain-copy-final-e2e.log`、`/tmp/fwx-plain-copy-final-browser.log`；截图`/tmp/fwx-plain-copy-ui-ppRc94/`。失败基线与重测分别保留在`/tmp/fwx-plain-copy-toolbar-before.log`、`/tmp/fwx-plain-copy-repeat.log`等本次临时日志中。
- 未提交、推送、发布；未改生产数据库、依赖、后端业务、权限或协议。正式管理员验收、真实手机Safari、硬件飞行、制造图纸接入、学校/课程管理不是本次完成内容。4–8起落架拼装与旧检查要求4个之间的规则差异仍保留待后续产品决策，不用文案掩盖。
