# 全站文案与按需提示

> 状态：本地实施及验收完成，未发布
>
> 更新时间：2026-09-14
>
> 适用范围：官网介绍、绘图/拼装/模拟/导出界面的文案与提示呈现
>
> 替代关系：按用户本轮决定收紧此前全站说明及预览提示方案；不改变校验、权限、数据契约或真实功能范围

## 目标与边界

- 去掉反复出现的制造/飞行免责声明，以功能、当前状态和下一步操作为主。
- 普通自制件自由摆放不产生常驻红黄警告；真正的来源失效、保存失败、连接问题仍可见。
- 绘图预览仅在失败时提供一条短原因、一条修改方法及必要操作；无错/空白/绘制中不出现右侧说明框，不重复多处播报。
- 导出报告保留全部检查及等级，仅直接显示错误和有修改方法的提示；正常项、数量和暂无数据合并为可展开的其他检查结果。
- 保留已确认的用户评价、用户自填内容、首页顶部及现有视觉风格。不编造新增能力或把校验失败写成成功。
- 复用 `flightwoodx-review`、`codex/home-hero-honors-2026-09-13`、HEAD `ea1f8ca` 的同一任务 WIP；无提交、发布、API或数据迁移。

## 实施

1. 检索实际用户可见文案，按官网/模拟/零件库、拼装检查、导出、绘图分工。
2. 先更新可失败的文案/状态回归，再修改呈现。校验代码与正式数据不变。
3. 实测390×844、768×1024、1440×900的正常及失败状态，检查提示精简、无重复、无溢出和恢复入口。
4. 运行目标测试、完整CI和浏览器回归，记录实际结果；完成后更新当前状态与文案规则。

## 依据与回退

- 用户截图及本轮要求是文案和显示决策来源。错误提示参考 [GOV.UK error message](https://design-system.service.gov.uk/components/error-message/)：说明问题与修正方法，保留输入，不把服务能力不足当成用户输入错误。
- 回退仅恢复本轮展示文字/样式和条件及对应测试，不撤销此前绘图、孔槽、保存和几何改动。

## 验收记录

- 文案目标测试先红后绿，覆盖官网/关于/模拟/零件库、普通自由拼装、导出说明和错误恢复。普通装配界面过滤 `EVIDENCE_MISSING` 的展示，不修改共享检查结果或成功条件。
- 首次CI发现新增测试夹具使用了不合法的实例类别 `deco`；按现有用户零件到实例的映射改为 `joint`，未放宽类型定义。最终完整CI为748/748，包含451项Web测试；日志 `/tmp/fwx-concise-copy-ci.log`。
- 读屏回归先确认预览区域被 `alert` 覆盖的问题，再将单一播报置于有名称区域内部；保留区域定位和错误播报。手机保存状态拆行先以失败断言确认，再补整段换行规则。
- 第一轮目标浏览器13/16通过。三项失败均因新增用例在引导第一步查找仅最后一步显示的“保存草稿”；改为观察添加主板触发的实际自动保存，不改界面逻辑。首批截图过早捕获入场动画/三维加载，后续增加实际显示与木板像素等待。
- 已查看三尺寸绘图反馈及390/768自由拼装截图，提示无裁切或重叠。独立视觉复核发现手机保存状态断行，已修复；两条成功消息对应保存和放入两个动作，保留行为，截图等其自然消失。导出空作品原有12块提醒已进一步精简为一个缺主板问题，其余检查按需展开。
- 最终完整CI748/748通过；浏览器新增三尺寸3/3通过（53.7秒），全站72/72通过（5.0分钟）。日志 `/tmp/fwx-concise-copy-ci.log`、`/tmp/fwx-concise-new-browser.log`、`/tmp/fwx-concise-all-browser.log`。故障注入之外无页面/控制台/网络异常；保存失败仍保留数据并可真实重试。
- 主代理与独立代理实际检查三尺寸反馈、自由拼装、稳定木板预览、官网/公司介绍及导出结果；截图 `/tmp/concise-*.png`。导出空作品只显示缺主板问题；无可操作问题时仅呈现计数和折叠入口。截图位置避开固定导航，不改产品样式。
- 完整72项通过后的额外截图重拍，手机/平板2项通过，桌面在新建隔离账号时触发既有同IP注册限流，未进入该次桌面工作流；日志 `/tmp/fwx-concise-final-capture.log`。未改或绕过限流，未重启API；此前桌面完整功能与三维预览验收结果保留。
- 桌面导出通过公开游客入口另行补图，1/1通过（5.9秒）：无认证请求、无API写入，空作品与添加主板后的标题、问题和折叠内容完整；日志 `/tmp/fwx-concise-guest-export-capture.log`。临时截图用例已移除，不增加永久测试范围。最终harness、目标ESLint、类型与diff检查通过。
- 本地4173读回 `index-BRg1QWvX.js`、`PartStudioPage-2cNBtdDn.js`。本轮未提交、推送或发布，未更改API、数据库、依赖、环境，未启动或重启后端；之前的孔槽/几何/发布准备WIP均保留。
