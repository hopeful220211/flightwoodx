# DJI 页面视觉对齐

状态：本地实现与验证完成，未发布
更新时间：2026-09-15
适用范围：FlightWoodX 全站前端视觉；不包含发布、后端或业务能力变更。
替代关系：本轮按用户提供 DJI HTML 对齐字体、版式和控件，替代原 MiSans / 钉钉进步体及蓝色展示风格；首页两行 Montserrat Black 品牌标题保留。

## 依据与边界

- 来源：用户提供的 `DJI 大疆创新 - 官方网站 (2026_9_15 00：41：33).html`，离线 Chrome 渲染，禁用脚本并阻止外部请求。
- 提取实际可见节点的计算样式及 390 / 768 / 1440 px 布局，不把隐藏弹层或轮播克隆误当当前样式。
- 保留本项目图片、文字、奖项来源、真实评价、备案、账号与工具交互。产品展示按参考重排；工具应用保留其工作区结构，因为提供的 DJI 文件没有二维/三维编辑器对应页面。
- 在独立 worktree 从 `0fca2c6` 开始；主目录未提交业务修改及 release worktree 的发布优化修改原样保留。

## 工作与验收

- [x] 记录字体、导航、标题、按钮、图片/内容网格、页脚、圆角与响应式规则。
- [x] 新设计约束测试先失败，再统一字体和基础样式；保留标题例外。
- [x] 重排首页、关于我们；对齐公共导航、页脚、账号、作品/社区与绘制/拼装控件。
- [x] 检查展示与工具页面三种尺寸的实际渲染、交互和溢出；对比参考并记录必要差异。
- [x] 运行针对性测试及完整检查/构建，更新状态与交付本地预览，不上线。

## 验证环境

Browser plugin not available。使用项目 Playwright 与本机 Chrome，专用本地端口及隔离测试数据，不写生产服务。

## 已取得的证据

- 目标设计约束3项先因缺少字体/章节/按钮规则而失败；实现后与首页7项共10项通过。后续目标覆盖视频，21项通过。
- `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 通过：962项（根203、共享105、Web548、API106），检查阶段30.5秒，Web构建约5.65秒；harness、类型、lint、安全均通过。日志 `/tmp/fwx-dji-ci.log`。
- 完整检查后仅统一视频失败重试按钮的颜色/字重并清除三处空白；针对性21项重新通过，Web再次构建5.64秒。未重复全套检查。日志 `/tmp/fwx-dji-final-build.log`。
- Chrome计算样式三尺寸检查3/3通过；实际CDP字形分别为OpenSansRoman-SemiBold、PingFangSC-Medium与MontserratThin-Black。字体文件本地加载，未改原Montserrat摘要。
- 已逐图查看 `/tmp/fwx-dji-final/home-full.png`、桌面绘图和作品页、手机首页与社区、平板关于页；12组页面/尺寸组合均无横向溢出。三尺寸截图与简表位于 `/tmp/fwx-dji-final/`。

## 测试环境纠正与失败记录

- 初次临时开发服务器从错误工作目录启动，Tailwind未找到本项目配置导致错误覆盖层；停止后从Web目录启动恢复。没有通过改掉样式断言处理该问题。
- 开发模式原视频组件仅声明autoPlay，StrictMode挂载/卸载期间可能暂停；观察到已加载但currentTime为0，手动play成功。最终回归使用正式构建，未为开发环境改写既有视频行为。
- 首次整体验收的专用API缺少PUBLIC_BASE_URL，使封面地址指向API端口；已将测试配置对齐预览4191。同一正式站点配置不受影响，未放宽同源断言。
- 首次桌面综合反馈用例记录一次 `Cannot read properties of null (reading 'addEventListener')`；该用例不改产品、不改断言的独立复测通过。测试错误现在保留堆栈，最终全流程继续检查是否复现，不把一次通过写成已修复此偶发错误。
- 第二批发现备案链接行高20px，与参考页链接24px及既有可点击区域检查不一致；已统一至24px。仅重建Web，关于/联系和备案重新验证，核心流程不从头重跑。
- 隐私用例要求本地统计服务开启，初始隔离API关闭统计导致提示不出现；仅在专用测试配置开启并生成独立随机密钥，测试真实同意/撤回/删除。没有修改产品默认开关或生产配置，没有删除隐私断言。

## 最终验收与交付

- 95个独立浏览器用例全部取得通过结果，按影响范围分批保留证据：第一批通过20项；页脚行高修正后通过61项（其中4项关于/联系重验）；最后启用隔离统计配置后18项通过。20 + 61 + 18 - 4 = 95，不把失败批次写成单次全绿。
- 日志分别为 `/tmp/fwx-dji-browser-final.log`、`/tmp/fwx-dji-browser-remaining.log`、`/tmp/fwx-dji-browser-final18.log`。最后Web构建5.81秒；没有改动线上配置，也没有在每次局部修正后重跑全部CI。`git diff --check` 与最终harness通过。
- 覆盖三尺寸页面、字体下载/恢复、真实注册、拼装与跨设备保存、编程/模拟、2 mm 木纹预览、实际SVG绘制、八个缩放锚点、悬停工具说明、独立插接口及方向保存、孔移出板外、普通矩形开孔、自动回选择工具、隐私同意/拒绝/撤回/删除失败重试、循环演示及手动视频错误恢复。
- 前述一次空值事件错误在独立复测和后续完整路径中均未复现；保留失败记录，不声称根因已修复。既有热更新与大型Three.js/Blockly构建提示保留。
- 预览 `http://127.0.0.1:4191/`。原4173预览、主目录未提交业务改动、release工作树的发布优化工作不受影响。
- 变更仅在独立工作树；未提交、合并、推送或部署。撤回本次改版时保留原工作树，停止使用此预览即可；未来上线先合并本分支差异并遵守固定发布程序，不直接覆盖生产目录。

## 2026-09-15 接续：三维空值事件错误

- 连续复跑桌面综合反馈流程，第二轮复现同一错误，堆栈确认为 Fiber Canvas 的迟到初始化，并非字体或木板几何问题。日志 `/tmp/fwx-dji-followup-browser-repeat.log`；第一轮通过、第二轮失败、第三轮按上限未运行。
- 新增使用真实 Fiber 的确定性回归，延迟渲染器初始化、移除画布后再完成初始化，原版本稳定抛出同一 `addEventListener` 空值错误。测试环境仅模拟尺寸及 GPU；额外验证仍挂载的画布正常启动。
- 给既有 Fiber 9.6.0 加入可随锁文件安装的最小补丁：初始化完成后确认 canvas 尚未卸载，再继续渲染。三个模块入口一致；无版本升级、API业务、正式配置或数据契约变更。说明在 `patches/README.md`。
- pnpm 仅安装 API 时也读取根补丁文件；按镜像输入建立的本地隔离安装确认缺文件会失败。给镜像两个安装阶段补齐 `patches/` 复制，新增配置回归先失败后通过；相同清单离线锁定安装通过，无额外下载或安装脚本执行。未执行真实容器构建，未修改 API 业务。
- 本轮最初的复测被上一轮隐私测试启用的统计提示遮挡，尚未到达目标流程；仅将专用临时 API 恢复统计关闭的目标环境，正式设置与产品代码未改。该失败与三维根因分开记录。
- 最终完整 CI 通过 965 项（根204、共享105、Web550、API106），harness、类型、lint、安全通过，检查34.7秒、Web构建6.70秒。日志 `/tmp/fwx-dji-lifecycle-final-ci.log`。依赖版本不变，锁文件仅增加补丁摘要及对应引用。
- 原失败桌面流程连续3轮通过（57.3秒），手机/平板综合反馈与手机核心流程3项通过（1.1分钟）；分别见 `/tmp/fwx-dji-lifecycle-repeat.log` 与 `/tmp/fwx-dji-lifecycle-core.log`。桌面跨会话恢复及自制件混合流程2项通过（42.8秒），见 `/tmp/fwx-dji-lifecycle-restore.log`。本次共6条不同用例、8次执行通过，无意外页面/控制台/网络错误；保存故障与来源删除404仍按原用例单独校验，未删断言。
- 本地首页重新读取身份、实际字体及截图，无错误覆盖层、横向溢出或控制台错误，截图 `/tmp/fwx-dji-followup-home.png` 已查看。仍使用同一隔离工作树；尚未提交或发布，真实云端容器构建留待发布CI。
