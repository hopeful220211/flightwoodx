# 2026-09-21 当前版本发布

状态：服务器已连接；首轮云端发现两处绘图回归，修复后重新核验，尚未上线。
更新时间：2026-09-21。
适用范围：已完成的全站设计细节、登录/绘图交互和自制接口对齐修复。
替代关系：记录本轮实际发布，不替代三项开发验收记录。

- 用户明确授权以最快速度发布当前版本；不扩大产品改动或服务器权限。
- 原生产前端 c785b94dd3040f198b73ab1f1cf2307770b5f210，API flightwoodx-api:a4e27af；候选沿生产单父历史提交，无数据库迁移。
- 复用已有本地完整CI1,028项及5项最终浏览器流程，不重复本地完整检查。推送最终候选后取得一次可信云端检查与归档。
- 共享geometry改变混合连接姿态校验，必须先更新兼容API，再发布同一测试前端；沿用既有Workbench管理通道，备份配置、数据库和上传卷，不创建通用SSH权限。
- 开始核对时间为2026-09-21 00:39（北京时间）。当前Chrome没有服务器终端，已请用户打开并连接；候选准备和云端检查可先执行。
- 未确认API更新、云端证据和正式站前，不报告上线成功。保留旧前端及API回退材料，不删除任何生产数据。
- 候选71d8423ead62cd03d9fbee7ceb8b2727696047f2已推送原开发分支，云端运行35523502577（https://github.com/hopeful220211/flightwoodx/actions/runs/35523502577）。没有重跑本地完整CI，也没有推进production。
- 已实际打开阿里云ECS入口，重定向到登录页；原SSH配置引用的临时身份文件已不存在。请用户扫码登录并连接既有Workbench终端后继续兼容API发布。后续复用同一候选运行，不重复上传/启动检查。
- 用户连接后已通过既有Workbench登录；精确候选API镜像已构建，目录`/root/fwx-release-20260921-0AAKQo`。未切换API、未操作生产数据库。
- 首轮云端其余检查通过，浏览器组1、2失败：平滑曲线选框用折线近似包围盒，偏离SVG曲线极值；测试在Linux固定使用Mac原生剪贴板快捷键。新增极值回归先失败，改为解析三次曲线极值；剪贴板测试按主机平台使用Command/Ctrl，不放宽断言。50项目标单测和ESLint通过，三尺寸原失败浏览器用例重新核验。
