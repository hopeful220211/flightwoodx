# FlightWoodX 项目规则

本文件是仓库级地图。模块细节写在离代码最近的 `AGENTS.md`，产品、质量和执行事实写入 `docs/`，不在这里重复维护。

## 1. 按 Harness Engineering 工作

开始任务时依次读取：

例行上传或发布已完成的改动，先执行 `pnpm release`，按[固定发布规则](deploy/automation/release-procedure.md)检查当前工作树、差异及当前提交证据；不重复开启产品调研、功能修改或历史全站审查。涉及继续开发、修复或发布程序变更时，仍按以下开发入口执行。

1. 本文件；
2. [`docs/index.md`](docs/index.md)、[`ARCHITECTURE.md`](ARCHITECTURE.md)、[`CURRENT_STATUS.md`](CURRENT_STATUS.md)；
3. 当前任务的产品规格、质量规则、接口契约和目录内 `AGENTS.md`；
4. 实际代码、测试和当前 Git 状态。

涉及网站设计、界面调整或新增页面，必须先读取[网站设计参考](docs/design/dji-reference.md)，并检查最接近的现有页面与共享样式。用户已确认白灰为主体、蓝色为点缀的 DJI 参考风格；没有新的明确要求时沿用既有字体、颜色、布局与控件，不另起一套视觉方案。用户后续指定的细节优先，确认后同步参考文件。

复杂任务在 `docs/exec-plans/active/` 建立可更新的执行计划，完成并验证后移入 `docs/exec-plans/completed/`。

项目采用以下原则：

- 需要跨任务长期采用的项目事实必须写回仓库并受版本控制；聊天记录和记忆只能作为待验证线索。
- 人决定目标、边界和取舍，代理负责实现、验证和留下证据。
- `AGENTS.md` 只做地图；稳定知识按模块放到文档、契约、测试和代码旁。
- 架构边界、安全规则和数据约束尽量由 schema、测试、静态检查和 CI 自动执行。
- 完成结论来自真实反馈：测试、浏览器、接口、数据库或目标环境；不能只凭代码外观。
- 同类错误重复出现时，修复代码的同时补强规则、测试或工具。
- 所有写入任务都在已验证、没有无关改动的隔离工作树中进行；独立任务使用 `codex/` 分支，重叠或并行任务再新建工作树。
- 持续清理废弃文件、重复规则、死代码和失效文档，避免仓库熵增。
- 先使用最简单且可验证的流程；只有明确出现协作瓶颈时才增加编排复杂度。

完整方法和证据要求见 [`docs/quality/HARNESS.md`](docs/quality/HARNESS.md)。仓库 Skill `flightwoodx-development` 负责引导执行，但不能覆盖本文件，也不能替代自动检查。

## 2. 事实与权限顺序

冲突时按以下顺序判断：

1. 用户在当前任务中明确作出的决定；
2. 本文件中的冻结规则；
3. 当前代码、测试和实测结果；
4. 已批准且仍适用的规格或 RFC；
5. 路线图、历史说明、截图和演示。

RFC、页面或 mock 的存在不代表功能完成。合并、推送、部署、生产数据删除、Git 历史清理、公开范围变化、权限放宽，以及飞行或制造保证，必须获得明确授权。

## 3. 产品真值与文案边界

当前核心闭环是：零件绘制或选择 → 拼装 → Blockly 编程 → 视觉仿真 → 保存 → 部分导出。详见 [`docs/product-specs/core-flow.md`](docs/product-specs/core-flow.md) 和 [`CURRENT_STATUS.md`](CURRENT_STATUS.md)。

- 规则检查、几何估算和视觉仿真只提供软件层面的辅助判断，不等于真实飞行、结构安全或可制造性证明。
- 大模型可以解释、建议和生成草稿，不能替代权限判断、确定性校验或最终安全结论。
- 发布、徽章或评分依赖规则检查时，服务端必须使用受控证据和同一规则版本重新计算；客户端 evidence 与 `verified` 状态不可信。
- 未经真实工程验证，不得写出“真能飞”“保证能飞”“一定安全”“保证可造”“可直接加工”等承诺。
- 首页现有文字在普通开发任务中保持不变；其中尚未被证据支持的表述仍是发布阻断项，也不得复制到新页面。

## 4. 模块与数据边界

| 范围 | 职责 | 就近规则 |
| --- | --- | --- |
| `apps/web` | React + Vite 前端、编辑器、作品界面、浏览器状态 | [`apps/web/AGENTS.md`](apps/web/AGENTS.md) |
| `apps/api` | Express API、鉴权、持久化和服务端编排 | [`apps/api/AGENTS.md`](apps/api/AGENTS.md) |
| `packages` | 可复用契约、零件、几何和飞行检查逻辑 | [`packages/AGENTS.md`](packages/AGENTS.md) |

- 应用可以依赖共享包；共享包不得反向依赖应用。
- 跨前后端的数据必须有运行时校验，不能只依赖 TypeScript 类型。
- `DroneDesign` 是已登录作品的服务端事实；程序通过 `programId` 绑定，不以浏览器缓存充当正式记录。
- 零件注册表是官方零件元数据的单一来源；社区、赛事和 fork 的存量兼容只通过 `Project` 桥接层处理。
- 标识符跨边界统一使用字符串。客户端提交的所有者、角色、公开状态、评分和审计字段均不可信。
- 大文件进入对象存储；数据库保存元数据、归属和可追踪引用。

## 5. 标准开发闭环

1. 确认工作树、分支、改动范围和基线结果。
2. 写清验收标准、数据、权限、失败路径和非目标；复杂任务建立执行计划。
3. 先写能失败的测试或检查，确认它因目标问题失败。
4. 在正确模块实现最小完整改动；不复制共享规则，不静默降级，不让 mock 进入正式路径。
5. 前端用真实浏览器验证，后端验证鉴权、越权、非法输入和失败路径。
6. 迭代运行目标测试。收尾核对当前版本完整 CI：云端已有有效结果则复用；未推送的本地交付运行一次 `pnpm run ci`（已包含 harness）。不要另跑一遍 harness，或在例行发布时重复完整本地检查。发布仍必须有可信云端检查与测试归档。
7. 同步规格、状态、执行计划、风险和决策记录。
8. 交付改动、证据、残余风险、回滚方式和下一步。

每次上传、更新、上线必须遵循[固定发布规则](deploy/automation/release-procedure.md)，使用 `pnpm release` 查看计划，得到发布授权后使用 `pnpm release --publish`。同一提交复用有效 CI 证据和同一构建产物；独立检查并行执行，阶段耗时与结果由程序记录。缺少证据必须补齐，不能靠跳过检查提速。不得把源码推送、CI 通过当作已上线，也不得把后端、数据库、权限或部署器升级夹带进前端发布。服务器和回退细节见 [`deploy/automation/README.md`](deploy/automation/README.md)。

发布目标：已有有效证据 2–5 分钟，需新完整 CI 12–18 分钟；以实测为准。完整检查只在最终版本运行，前端只构建一次；三个普通浏览器分组和一个隐私分组全部通过后方可发布。线上仅验收改动路径；三尺寸只补布局检查，不重复全部业务流程。默认等待上限 18 分钟，超时保留作业链接，禁止自动重跑、重复上传或扩大任务。完整时限、停止条件与额度控制以固定发布规则为准。

## 6. 安全与未成年人保护

- 密钥只来自环境变量或受控密钥系统，不写入仓库、日志、截图或前端包。
- 日志不得泄露密码、令牌、完整手机号、未成年人身份或作品中的敏感内容。
- 服务端在每次受保护操作中验证身份、角色、资源存在性、所有权和状态；不能信任客户端声明。
- 所有外部输入都要限制格式、大小、速率和路径；错误响应不得暴露内部堆栈或密钥。
- 涉及学生和未成年人时，不默认开放私信、真实姓名、精确位置或外部链接；上线前完成隐私与内容安全核验。
- 数据迁移、破坏性脚本和公开范围改变必须有备份、回滚方案和明确批准。

完整安全基线见 [`docs/quality/SECURITY.md`](docs/quality/SECURITY.md)。

## 7. 验证命令

```bash
pnpm run harness
pnpm typecheck
pnpm check:api
pnpm test
pnpm lint
pnpm security
pnpm build
pnpm run ci
```

不得通过跳过测试、放宽规则、隐藏错误或删除断言制造绿色结果。

## 8. 完成定义

- 正式路径使用真实数据，没有未说明的 mock、占位实现或静默回退。
- 数据、权限、错误、重试、刷新和多端一致性均有明确行为。
- 改动位于正确边界，并有能阻止回归的自动测试。
- 当前提交上的完整 CI 通过；涉及界面时还要验证 `390×844`、`768×1024`、`1440×900`。
- 产品表述与实际证据一致。
- 相关规格、状态、执行计划和风险已同步。
- 数据库、环境变量、迁移、安全和回滚影响均已报告。

自动检查通过只证明仓库满足当前门槛，不等于产品已达到发布条件。
