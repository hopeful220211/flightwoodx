# FlightWoodX design guide

状态：2026-09-20接入 awesome-design-md；本站样式以用户确认的 DJI 参考为准
更新时间：2026-09-20
适用范围：全站页面、编辑器、后台、弹窗、表单及加载/空/错误状态
替代关系：本文件是设计任务入口，不替代 [完整视觉参数](docs/design/dji-reference.md) 或产品行为规则

## 1. Visual theme / 视觉基调

白灰主体、蓝色操作点缀，产品与作品图片优先。保留现有所有文字、照片、模型、信息顺序、页面结构和功能；此规范不授权改写内容或发布生产版本。

采用 [awesome-design-md](https://github.com/VoltAgent/awesome-design-md) 的 Markdown 设计约束方式，并参考其 Apple 分析中的文字层级、分用途圆角、控件状态与响应式检查。它是社区分析合集，不是品牌官方标准，也不是自动美化插件。

安装版本固定为 `8147538b4226ae41e2487a9179e3bcc1f68e8554`。说明、MIT 许可证及选用原文保存在 [vendor/awesome-design-md](docs/design/vendor/awesome-design-md/README.md)。无运行时包、网络字体或上游脚本。不能同时套用多个品牌冲突的规则；不采用 Apple 的 SF Pro、配色、17px正文、黑色导航或新布局。品牌参数继续来自本站 DJI 参考。

## 2. Color roles / 色彩

颜色保持既有值：白 `#fff`，灰底 `#f7f9fa`，次表面 `#edf1f4`，线 `#d9dfe3`，主要文字 `#303233`，行动蓝 `#0070d5`，悬停 `#005eae`。不批量替换原有功能色：孔槽/删除红、成功绿、提醒橙、木材、奖项、Blockly分类色均保留。使用已有 Tailwind/变量，不另造色板。

## 3. Typography / 文字

Open Sans + 系统中文回退，常规400、中等500、标题600；字体文件不变。首页大型 FLIGHT / WOOD X 的 Montserrat Black 900、尺寸、字距及动画不变。

正文16/24，紧凑文字14/20，辅助文字至少12/20；不要把旧9–11px标签作为新默认。普通页面标题桌面32/36、手机28/32，章节与首页仍按既有层级。普通字距-.02em，页面标题-.03em，辅助标记normal，不额外拉宽中文。协议15/32、左对齐。数字比较用tabular-nums，不修改数值。

## 4. Components / 控件

复用公共 Button/Input/Select/Modal/Table/Tabs/PageHeader。普通操作44px、较大操作48px；按钮文字14/20，图标16–20px。输入框44px、小圆角4px；触控普通表单16px文字。编辑器紧凑属性输入是保留例外，不机械扩大而遮住画布。

按钮保留default/hover/focus/disabled/loading，蓝色2px键盘焦点，不在选择/悬停时改尺寸。圆角：输入4、工具按钮6、弹窗8、现有营销按钮64、头像圆形、大图0。弹窗桌面正文24、手机16；表格14/20，数字对齐，窄屏在自身容器滚动。

## 5. Layout / 排布

保留每个页面的现有分区、图片位置、网格顺序和导航。复用1200px内容宽度，桌面24px/手机16px边距；间距4/8/12/16/24/32/40/48/64。同级内容同一对齐线；名称、错误和长文本可换行，不用删字解决溢出。注册说明指定断行不变。

## 6. Elevation / 层次

内容主要靠留白、分隔线与文字层级。浮动工具/弹窗沿用 `0 4px 20px rgba(0,0,0,.1)`；不追加装饰发光、渐变或浮起位移。图片、材质和既有首屏背景不变。

## 7. Guardrails / 保留边界

- 不更改正文、按钮名称、署名、奖项、价格、链接目标、图片路径、数据或权限。
- 不更改2mm几何、木纹、拼装规则、绘图坐标、缩放锚点、自动选择和保存。
- 不把展示页排版套进编辑器；二维/三维标题对齐、底部工具条及固定属性面板保持。
- 新设计规则遇到已确认的局部规则时，保留局部规则并记录例外。
- 不复制上游品牌商标、图片、专有字体或宣传文案。

## 8. Responsive / 响应式

验收390×844、768×1024、1440×900；检查长文字、所有状态、表单、对话框与工具栏，不只截首页。手机表单防自动放大；触控目标优先44px；画布命中区独立于可见锚点。尊重减少动态效果，不新增动画。第三方 Blockly/CAD画布坐标不受全局文字选择器影响。

## 9. Agent workflow / 使用方式

每次界面任务先读本文件和 [dji-reference.md](docs/design/dji-reference.md)，再查对应页面及共享组件。按共享样式→页面例外→三尺寸实际检查处理；不要重新挑选风格。

本轮只允许样式改动，用 `node scripts/check-design-content.mjs 34c6887` 对照任务起点检查 JSX内容、行为及资源引用；与测试/浏览器证据一起记录到执行计划。上游更新须显式选择新版本并对比规则，不能自动覆盖本站规范。
