# FlightWoodX 当前状态

> 状态：首屏荣誉图片替换完成本地验证，尚未发布；已有前端自动发布和三奖更正记录保留，正式管理员等业务验收仍待完成
>
> 更新时间：2026-09-13
>
> 验证对象：本次本地候选为 `codex/home-hero-honors-2026-09-13`（荣誉替换基线 `65b1510`，继续排版微调）；远端 `production` 仍为 `77e4a194ad81d711479df86d39f7e3c468963ad3`，本次未推送或部署
>
> 适用范围：当前代码、自动测试、真实浏览器、隔离数据库与正式网站部署边界
>
> 替代关系：更新 2026-08-17 状态；架构、冻结规则和历史交付分别见 `ARCHITECTURE.md`、`AGENTS.md` 与已完成执行计划

## 1. 当前结论

### 2026-09-13 荣誉图排版微调

- 按用户最终更正，以第三至第四张的原可见间隙为参考收紧前两处，不再专门缩小第三至第四张。只改 `HeroHonors` 的显示位置与尺寸，四份原图、其他文案和页面功能不变。
- 图片透明留白不同，使用相对自身宽度的显示偏移补偿，未编辑或裁切图像。1440×900 实测三处可见间隙由约 31.5 / 26.9 / 21.1px 调整为约 21.7 / 21.7 / 21.7px；单图宽由156.8px增至164.8px（约5.1%），图片顶部由98.3px下移至108.8px（约10.5px）。下移使用独立定位，不干扰已有入场动画。手机继续两列并适度收紧每行的横向间距，平板/电脑保持四列，页面无横向溢出。
- 新的浏览器布局断言先在旧版1440视口失败：可见间距差10.45px、未下移、尺寸未增大。调整后生产预览 `http://127.0.0.1:4173` 上三视口回归3/3通过（最终9.6秒），覆盖每张图增长、可见间隙、桌面/平板参考间隙基本不变、无重叠、按钮与标题、鼠标/Enter跳转。Browser plugin not available；使用项目既有 Playwright 测试入口，并用 CUA 实际查看390×844、768×1024、1440×900。页面身份正确，无加载错误页、控制台错误或失败请求，键盘跳转实测到达下方奖项区。
- `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 完整通过，462项测试、API无跳过、审计无已知漏洞。最终入口 `index-Ry9bFXcb.js`，原Blockly大包提示保留；独立只读复核未发现阻断问题。没有改依赖、数据库、环境或服务器；没有推送和发布。本轮不重复声称已完成全业务E2E或正式站验收。

### 2026-09-13 首屏荣誉图片

- 按用户提供的四张 PNG，仅将首屏奖项文字胶囊替换为 Red Dot、iF、IDEA 和其他奖项的荣誉图片。原图存于 `apps/web/public/resource/picture/honors/`，已逐一与附件核对 SHA-256，字节完全一致；沿用现有流程生成同源 WebP，没有重绘、裁切或添加获奖级别。四张新增页面图片合计约 622 KiB。
- 原按钮改为 `HeroHonors`：手机两列，640px 起四列；保留点击/键盘跳转到下方奖项区、可见焦点和减弱动画支持。下方三奖介绍及其他首页文字不变，不恢复 G-Mark 展示。原 `AwardCapsule` 组件已替换，无残余引用。
- 组件回归先失败（旧版无四图荣誉按钮），替换后 2/2 通过。新增 `apps/web/e2e/home-honors.spec.ts`，覆盖 390×844、768×1024、1440×900 的四图实际加载、比例、列数、无横向溢出、首屏操作按钮、旧文案移除和点击/Enter 跳转；开发预览 3/3 通过。CUA 实际查看三个尺寸，图片完整、未遮挡标题/按钮，页面无控制台错误；浏览器自动回归通过项目既有 Playwright 入口执行，没有借用用户登录资料或写正式数据。
- 首次完整 CI 在依赖审计阶段发现 2 项高危、2 项中危工具链公告；没有跳过安全检查。仅补丁更新 sharp 0.35.4、js-yaml override 4.3.2、Vitest/配套 mocker 4.1.11，未改业务依赖或 API 逻辑。依据：[sharp](https://github.com/lovell/sharp/security/advisories/GHSA-rgj7-g3m4-5g8c)、[js-yaml](https://github.com/nodeca/js-yaml/security/advisories/GHSA-2883-xcg3-v3hh)、[Vitest](https://github.com/vitest-dev/vitest/security/advisories/GHSA-82fw-gwwq-j7x9)。
- 补丁后 `pnpm run harness`、`FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 全部通过：462 项测试（123 根脚本、61 共享包、198 Web、80 API），API 无跳过，审计无已知漏洞。构建入口 `index-Dns4XflL.js`；保留已有 Blockly 706.52 kB 大包提示，未放宽阈值。独立只读审查未发现本次图片替换的阻断缺陷。
- 最终生产构建预览 `http://127.0.0.1:4173` 再跑 `FWX_E2E_BASE_URL=http://127.0.0.1:4173 FWX_E2E_BROWSER_CHANNEL=chrome pnpm --filter web test:e2e home-honors.spec.ts`：3/3 通过（9.2 秒）；三个尺寸无失败请求或页面错误，含鼠标与 Enter 跳转。CUA 重新加载确认入口为上述最终构建并查看三种尺寸。此次仅运行新增首页浏览器回归，没有重新执行会写入隔离账号的完整核心 E2E 或本机 Docker smoke；发布前仍须通过远端全部 8 项门禁。
- 本轮没有修改数据库、环境变量、服务器权限或正式站，未创建正式账号。发布授权、确切提交的远端 8 项检查与正式回读尚待完成；不能把上述本地结果写成已上线。回滚范围为本次前端版本，不涉及业务数据迁移。

### 2026-09-08 前端自动发布

- 用户批准配置 GitHub 到现有 ECS 的自动发布。保留默认分支 `main`，新建 `production`，要求确切提交的 8 个 CI 检查全部通过、禁止强推/删除、管理员同样受限。生产环境 `ecs-production` 只允许此分支；专用私钥与可信主机公钥已保存为环境 Secrets，未写入源码。
- 浏览器测试后的同一份前端构建包含提交标识和逐文件摘要；受限 SSH 账号只允许发布、查询状态及条件回退。固定 root-owned 发布器只切换 nginx，不能从上传包执行命令或更新自身，不改 API、数据库、上传卷、现有账号、证书或防火墙。详见 [发布说明](deploy/automation/README.md)。
- 本地客户端 14 项、服务器 31 项、初始化 12 项测试通过，包含恶意压缩包、权限、失败补偿、事务恢复、重复发布及回退验证。完整实际 dist 打包并由服务器解包器检查通过：883 个公开文件、918 个解包条目，归档 142,885,335 字节；空 `.gitkeep` 源码占位保留但不发布。
- 用户已在 Workbench 完成固定提交、逐文件校验的首次安装。初次预检发现原 ACME 路径的四层目录归 UID 501 所有；仅将这四个目录非递归改为 root，保留 0755 和目录内容，原所有权记录在服务器 `/root/fwx-ownership-before-jhyjdl`。未放宽发布器检查、改动证书或重建生产容器。安装后真实受限 SSH 的 `status` 成功，任意命令、PTY 和端口转发分别被拒绝；主机密钥与可信记录及用户提供的服务器公钥一致。
- 本机 Harness、完整 CI 通过：462 项（根脚本 123、共享包 61、Web 198、API 80），另直接运行 43 项 Python 测试通过；这些 Python 用例已由根脚本中的两个包装测试覆盖，不重复累计。API 集成无跳过，依赖审计无已知漏洞，保留 Blockly 大包提示。确切 SHA `77e4a19` 的[开发分支 CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34234102951) 8/8 成功后，普通快进推入 `production`，没有绕过保护。
- [首次正式自动发布](https://github.com/hopeful220211/flightwoodx/actions/runs/34237204343) 于北京时间 2026-09-08 22:21:17 确认成功：8 个检查和 `Publish tested frontend` 全部通过，浏览器 4/4（1.7 分钟）；服务器与独立客户端均确认正式 SHA、首页及入口摘要和 API 健康。正式产物 ID `10060519837`，归档 SHA-256 为 `b4bdc24691803e568a58213f24ab12a308d3fd70a8a4af287a737dce69b78129`，公开清单 883 个文件；归档清单与线上清单已对账，不声称独立下载了全部线上文件。
- 首发后真实执行条件回退：状态从 `current=77e4a19 / previous=null` 变为 `current=null / previous=77e4a19`，旧首页及入口摘要与切换前完全一致，API 仍为 `OK / connected`。旧目录没有 `release.json`，因此 `null` 是未编号旧版，不是丢失版本。随后发送上述同一测试归档，重新确认 `current=77e4a19 / previous=null / ready=true`；新版已恢复。每次切换由固定服务器发布器核对 API/Mongo 容器身份未变，客户端另核对公网版本；没有额外取得通用服务器命令权限。
- 这是受控回退及恢复实测；故障触发的自动补偿、事务恢复和重试由隔离测试覆盖，没有在正式站故意注入故障。nginx 切换可能短暂断开连接，不称零停机。完整结果和后续维护边界见[实施记录](docs/exec-plans/completed/2026-09-08-automated-web-release.md)。

### 2026-09-08 奖项更正

- 用户明确未获得 G-Mark：移除其展示及首屏提及，仅保留 Red Dot、iF Design、IDEA；四奖说明改三奖，首屏 `10+ 项` 改 `3 项`。前三项原有级别、年份及图片不变；不扩大到其他页面文案。
- 奖项布局调整为手机一列、平板/电脑三列。新增 2 项真实组件文案回归，修改前按预期失败，修改后通过；完整 `pnpm run harness` 和 `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` 通过，446 项测试（Web 198，其余 248），API 集成无跳过，依赖审计 0 个已知漏洞。保留已有 Blockly 大包提示。
- 本地生产构建 `http://127.0.0.1:4173`：实际 Chrome 检查 390×844、768×1024、1440×900，三枚图片可读、数量及文字一致、无横向溢出，首屏奖项按钮可滚动至该区域；无页面/控制台错误或失败请求。Browser plugin not available，按前端验收 Skill 使用独立 Playwright 浏览器；未复用用户的登录资料。
- 当前候选入口 `index-VGPvOoS_.js`；首页 SHA-256 `45e682ef34c0f6492d2ed8b42200d992826b85a17b3258b888bea4e86772206d`，入口 JS SHA-256 `e8626c71aa8b530297f7d6a0d3527698dbc1b7b3f203728102e5d9d083258d30`。
- 三奖更正已随 `77e4a19` 上线，正式入口为上述 `index-VGPvOoS_.js`，首页与入口摘要均相同。正式 Chrome 独立会话在 390×844、768×1024、1440×900 通过：三枚奖项图片完整可读、首屏 `3 项`、无 G-Mark/四奖/10+ 残留、按钮滚动正确、手机一列/平板电脑三列、无横向溢出，无页面或控制台错误、警告及失败请求；截图实际查看通过。未借用用户登录资料，也没有新增正式账号或业务数据。

### 2026-09-07 已上线修复与验收边界

官网图片与模型资源、官方零件拼装、积木编程、视觉仿真、作品保存、登录注册、个人资料和社区主流程已修复并在本地验证。自制零件现可进入自由作品并按账号恢复；后台增加真实只读审计记录。首页主要文案未改。最新完整 CI 为 **444/444**（包括后补命名、封面、权限、nginx 路由及异常保留数据检查），最近一次生产构建浏览器回归 **4/4**；完整依赖审计未发现已知漏洞。

**2026-09-07 当时上线前端为 `7c3fb21`，入口 `index-D9E4hbkC.js`，时间为 04:21:19（北京时间）；API 为 `flightwoodx-api:f5b12b3`。** 此前端现作为自动发布的初始回退版本保留，API 未随 9 月 8 日前端更新而重建。该次候选的首页/入口 JS 摘要与本机验证构建一致，919 个公开条目权限通过；只切换 nginx 前端目录，API 和 Mongo 未重建。此前 02:33:58 发布 API，03:17 修复模型 0600 导致的 403，04:13:24 修复工作台同名目录截获路由；没有重置密码或修改密钥、证书、备份目录权限。

正式浏览器注册、六件官方拼装、积木云端保存、仿真运行/停止/重置/10 秒完成，以及全新无痕窗口登录后恢复作品和程序均已通过。03:20 重建 API 后及最终前端上线后，重新登录读取专用混合作品、来源零件、绑定程序及新上传封面全部成功，封面字节一致；Mongo 容器身份未变。最终版本的首页与两个工作台路径均 200/no-cache，244 个正式资源、5 类缺失资源 404、数据库健康和跨账号拒绝全部通过；正式 Chrome 强制刷新仍能恢复游客草稿及木色封面。

仍不宣称整个平台全部完成：正式管理员完整验收、社区写入与自制绘图的正式浏览器全流程仍未闭环。中文选词提前提交由代码审查和失败测试确认，不把控制工具无法输入中文误报为浏览器复现。旧版浏览器缓存可能仍保留七月入口；首次验收使用强制刷新或带版本参数的首页，避免把旧入口当成当前版本。

生产数据库、配置、证书、旧站、API 镜像和上传文件已备份；独立无网络容器恢复数据库，20 个集合记录数全部一致。切换前停旧 API 后再次备份并核对全部 6 个旧上传文件。OSS 控制台显示未开通，原凭据返回 403；未购买服务或更改桶权限。正式上传已改为磁盘持久卷，原配置与旧站保留。

## 2. 本轮改动

| 范围 | 已落地改动 |
| --- | --- |
| 图片与资源 | 35 张原图约 101.1 MB 生成约 3.8 MB WebP，原图保留；官方模型/缩略图/纹理不再从部署包删除；移除远程 Google 字体，Blockly 图片和声音同源提供并附许可证 |
| 拼装 | 唯一 ID、步骤推进、父子零件删除/翻转/连接点、双主板位置、异步添加数量与文档归属、鼠标/触控一致性、镜头适配、模型失败重试 |
| 编程与仿真 | 空/无效/未完成草稿保存，防旧程序复活；程序与作品关联；服务失败不盲目新建；仿真读取当前程序、停止/重置/再运行、实际拼装模型显示、缺条件与运行错误可见；慢回调按实际时间推进并检查完整运动路径碰撞 |
| 保存与账号 | API 用户响应一致；昵称/学校/年级真实保存；账号切换不串资料/作品；迟到登录/资料响应被拒；所有作品保存入口按账号与作品串行，切页刷新、同账号换令牌及删除顺序有回归 |
| 社区与权限 | 公开作品撤回后，详情/评论/榜单/作者/收藏/关注及旧兼容入口不再泄漏内容；非法字段与重复注册使用 400/409；学生不能凭管理密钥越权 |
| 后台用户 | 既有真实 API 接入只读用户列表、搜索、角色筛选、分页、空态和重试；未增加角色修改或学生管理工作流 |
| 界面 | 桌面/平板/手机布局、编辑器三种模式导航、登录弹窗在编辑器可用、加载与错误状态、资料编辑入口、零件删除确认、弹窗焦点、手机评论操作、中文零件类别 |
| 请求限制 | 有效签名账号分别 600 次/15 分钟，未认证 100/IP，登录/注册/改密码 20/IP；配额识别不代替鉴权。避免持续编辑及同校出口共享极低额度 |
| 发布配置 | 同源资源真实 404、上传反代与磁盘持久卷、一层可信代理；远端资产探测失败时明确改用本次同源构建；API 健康检查与升级/回滚说明；Node 22 与独立容器检查；更正证书申请顺序、文件路径及仅数据库备份边界 |
| 质量与安全 | 加入真实 MongoDB API 集成和可重复浏览器主流程；修补 sharp、browserslist、qs、fflate、postcss-selector-parser 已知漏洞；没有跳过失败测试 |
| 自制件接入 | 自由作品保存来源 `id/version/updatedAt`、实际几何和位置；与官方件混合保存、新浏览器恢复；源修改/删除/无权限明确提示，引用不丢；不伪造卡扣或历史几何 |
| 补充安全与检查 | 封面清理按服务端作品 ID 限定路径，阻止跨用户删除；OSS 使用 HTTPS，上传/清理错误不记录原始 SDK 凭据；修正未实际覆盖页面源码的类型检查入口并补防回归检查 |
| 参数证据 | 移除无来源的 25/35g 飞行阈值、默认 2g、电机数等于机臂数、坐标均值等于重心等结论；只显示目录估算、数据覆盖率和可核对的结构引用 |
| 后补交互与封面 | 中文选词 Enter 不提前提交；改名不在 React 更新函数内写外部状态；纹理就绪后显式渲染再截图；自制/混合件完整进入封面，小尺寸模型调整相机裁剪；账号/快照/项目绑定与迟到回调隔离，失败不上传空图，保留重试 |
| 异常恢复 | 页面异常不再无条件清空本地存储；保留登录、未同步作品/程序及其他偏好，只提供刷新重试。不能据此恢复旧版本已经删除的数据 |

原始 `chore/add-speckit` 工作树及无关视频/本地文件未被修改。仓库继续保留原有 Harness 规则、分层契约和 FlightWoodX 项目 Skill。

## 3. 2026-09-07 验证证据（历史）

本地运行环境：Node 22.23.0、pnpm 9.12.0、MongoDB 7.0.24、实际 Chrome。自动回归使用隔离数据库；正式 HTTPS API 验收另外创建 2 个独立测试账号，正式浏览器注册再创建 1 个，仅新增私有测试作品、零件、程序和封面，没有删除已有数据或创建公开测试帖子。

| 检查 | 结果 |
| --- | --- |
| 冻结依赖安装、Harness、类型、API 语法、lint、密钥扫描 | 通过 |
| `FWX_TEST_MONGO_URI=mongodb://127.0.0.1:27028 pnpm run ci` | 通过 |
| 自动测试 | 444/444：根脚本 107（含 Harness 101、类型检查入口、公开文件权限和 nginx 路由回归），shared 28，geometry 16，parts-schema 11，flight-check 6，Web 196，API 80；API 集成未跳过 |
| `pnpm audit` | 0 个已知漏洞；不是无漏洞保证 |
| 生产构建 | 通过；保留 Blockly 约 706 kB 的大包提示，没有调高阈值隐藏 |
| 实际浏览器 | 注册/重登录，官方 6 件拼装保存刷新，绘制闭合轮廓→3D→保存刷新，积木示例/清空/恢复，仿真运行停止重置完成 |
| 实际社区浏览器 | 发布、点赞、评论、创建合集收藏，刷新后保留 |
| 实际后台与个人资料 | 管理员登录/密钥/3 条真实测试用户/教师筛选/空搜索/刷新；学生持密钥仍 403；昵称学校年级保存刷新、503 保留编辑值 |
| 三个视口 | 首页、拼装、编程、仿真、个人资料与用户管理检查 390×844、768×1024、1440×900；无横向溢出；首页图片与视频实际可读 |
| 故障与数据边界 | 模型 503 后重试；程序 503 不丢草稿；同账号换令牌串行、切账号取消排队写；连续 110 次真实作品保存 200 |
| 慢速浏览器 | CPU 降速 12 倍，同一约 10 秒程序修复前 18.0 秒、修复后 10.1 秒；后者定时器平均间隔反而从 88.6 ms 增至 92.6 ms，证明修复不依赖更快回调；终点正确、无页面异常 |
| 真实 API 容器 | GitHub 独立作业实际构建 Node 22 生产镜像，容器启动并返回 `API container health: OK; MongoDB connected` |
| 固定生产浏览器回归 | 见下方最终执行记录 |
| 正式 ECS/nginx/存储 | 新 API/nginx 已启动，Mongo 未重建；备份恢复/增量上传核对/证书 reload 通过；244 资源读取通过，模型 403 关闭；API 重建后私有作品/程序/来源与新封面仍可读 |

浏览器回归入口为 `apps/web/e2e/core-flow.spec.ts`，覆盖独立账号的桌面和手机主流程；桌面另创建一个无作品/程序本地缓存的浏览器上下文，只用登录会话从 API 恢复。第三项只读检查注册表全部 94 项零件的 188 个模型/缩略图：同源、禁止重定向、HTTP 200、非 HTML、GLB/PNG 文件头、长度和尺寸。测试拒绝生产地址和外部网络请求，关闭可能记录注册密码的 trace/video/DOM dump。CI 已增加真实 MongoDB 服务与独立 Browser core flow 作业，远端执行结果不由本机通过推定。

开发、CI 与 Docker API 镜像统一为 Node 22，最低 22.12.0。独立 API container smoke 作业实际构建生产镜像并连接临时 MongoDB；它与宿主机直接运行 API 的测试分开，不接触生产环境。本机没有 Docker，容器运行结果必须读取远端作业，不能由配置语法检查代替。

### 最终执行记录

- 最新本机完整 CI：通过，444 项。真实 app/node 源码类型检查、API、lint、依赖审计、构建通过；公开 dist 的 919 个条目权限统一完成。异常保留数据补丁的构建入口为 `index-D9E4hbkC.js`，浏览器 4/4（1.2 分钟；桌面 19.0 秒、手机 22.7 秒、资源 0.7 秒、自制混合件 28.7 秒）；该候选已于 04:21:19 正式切换。
- 后补封面浏览器回归：4/4 通过，约 1.3 分钟；桌面 18.1 秒、手机 23.7 秒、188 资源核验 4.2 秒、自制混合作品 29.5 秒。直接读取生成图片自身像素，确认官方、纯自制和混合件非空/非黑，并验证封面上传 200、存储地址同源、读取字节与上传相同。旧版纯自制封面在相同断言下失败，木色像素占比为 0；不把叠加的草稿徽标算入图片内容。无意外网络失败或页面异常；只有主动删除本测试来源后的同一 ID 404 被严格列作预期错误。之后补的 GLB 失败缓存重试已通过真实加载器先红后绿测试，最终构建浏览器再跑结果单独记录。
- GLB 缓存补丁后的最终生产构建 `index-DqEFyc6i.js` 浏览器复跑：4/4（1.2 分钟），桌面 17.7 秒、手机 23.0 秒、188 资源 0.7 秒、自制混合作品 29.2 秒；包括中文命名刷新/新会话恢复、三类封面实际像素/上传/字节核对及纹理警告回归。
- 收尾修复提交 `e47f0ca` 的[远端 CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34056139405)：8/8 通过；浏览器 4/4（1.9 分钟），桌面 27.3 秒、手机 26.5 秒、资源 1.3 秒、自制混合作品 56.6 秒；API 生产容器真实启动并连接独立 MongoDB。正式切换结果单独记录，不由远端 CI 推定。
- 04:00:59（北京时间）正式 nginx 已切至独立候选目录 `/root/flightwoodx-web-e47f0ca`，前端入口为 `index-DqEFyc6i.js`；首页及 JS 的 SHA-256 与本机验证构建一致。API 仍为 `flightwoodx-api:f5b12b3`，API/Mongo 容器 ID 与创建时间未变，证书和原 ACME webroot 保留。正式资源再次 244/244、5 类缺失资源 404；重新登录读取专用混合作品、来源、程序、跨账号拒绝与封面逐字节回读通过。
- 新前端正式冷访问发现 `/dashboard` 被同名公开目录截获，301 到 `/dashboard/` 后 403。模板删除 SPA 的目录候选 `$uri/`，仅匹配文件后回退入口；3 项配置回归先失败后通过，现有静态 404、缓存、TLS、API/上传反代保持不变。实际 nginx 回环验证与正式路由切换尚待执行。
- 路由提交 `cc23746` 的[远端 CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34057016714) 8/8，浏览器 4/4（1.9 分钟）通过。正式机临时 nginx 仅绑定 `127.0.0.1:18443`，使用原证书且未跳过验证；工作台有/无尾斜线均 200、HTML 摘要与候选一致、PNG/GLB 文件头正确、缺失资源 404、API 健康 200。04:13:24（北京时间）只重建正式 nginx；公开网址 13 项路由/资源状态验证通过，API/Mongo ID 和创建时间未变。
- 同一普通浏览器随后从缓存加载七月旧入口 `index-CYcC3otM.js`，日志确认仍请求 OSS 并触发错误页清空本地存储。带版本查询参数重新请求首页后加载新版，正式响应入口仍是 `index-DqEFyc6i.js`、`Cache-Control: no-cache`；没有将浏览器扩展的连接错误计为应用缺陷。另确认现有源码也残留无条件清空逻辑，已删除并补 3 项 jsdom/React 错误边界回归，验证登录及草稿逐字保留、刷新只调用一次且不清存储。该后补修复尚待最终发布，不声称恢复已被旧版清掉的数据。
- 异常保留数据提交 `7c3fb21` 的[远端 CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34057370749) 8/8，通过真实 API 容器检查及浏览器 4/4（1.8 分钟；桌面 27.5 秒、手机 26.1 秒、资源 1.3 秒、自制混合 53.3 秒）。
- 路由修复后正式 Chrome 实际直接访问工作台，未登录时正确显示登录入口；进入游客模式、新建单主板测试草稿、木质模型与封面显示正常，直接刷新后草稿和木色封面仍保留。此项只证明游客本地恢复，不算账号云端持久化；没有因此新增正式测试账号或公开帖子。
- 最终 `7c3fb21` 在服务器独立目录构建成功（1 分 13 秒），919 条目权限与首页/JS 摘要通过后，于 04:21:19 仅切换 nginx 至 `/root/flightwoodx-web-7c3fb21`；前一个 `e47f0ca` 候选和原 dist 保留。正式回读首页、`/dashboard`、`/dashboard/` 均 200/no-cache 且摘要一致，新 JS 200 且摘要一致；API 健康为 connected，244 个资源及 5 个缺失资源、专用私有数据与封面逐字节回读再验通过。API/Mongo 的容器 ID 与创建时间仍未变。
- 正式 Chrome 强制刷新最终版本后，游客单主板草稿和木色封面仍正常；随后仅删除本轮创建的 `Review Guest Preview` 游客测试草稿（不可恢复），退出游客模式，并将官网保留在登录窗口等待用户本人登录管理员。此前 3 个正式专用测试账号及其私有数据保留，没有删除用户已有作品。临时 nginx 验证容器已停止并移除，没有删卷。
- 以下是此前各次远端执行的历史证据，最新改动的远端 Checks 需另行读取，不能由旧记录推定。
- 首次远端 GitHub CI（`fa6c34c`，运行 `34048170907`）：6 项工程作业通过；浏览器手机流程通过，桌面仿真等待完成超过 45 秒而失败。该失败保留，不改成通过。
- 远端复跑（`a168721`，运行 `34048490808`）：8/8 作业通过，含真实 API 容器和浏览器 3/3（约 1.5 分钟）。桌面完整流程耗时 52.3 秒、手机 32.6 秒；这次通过不能证明不存在慢回调问题。
- 随后实际浏览器复现并修复定时器迟到导致的累积变慢，增加 5 项回归；最后本机生产浏览器 3/3，独立核对碰撞与停止边界通过。未调整原有完成断言、超时或重试数。
- 最后应用修复提交 `b456ab6` 的[远端 CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34048765595)：8/8 作业通过，含真实 API 容器与浏览器 3/3（54.1 秒；桌面 24.4 秒、手机 26.8 秒、资源 1.2 秒）。之后只修订发布说明及装机脚本末尾提示；当前分支最新提交的远端结果仍应读取其 Checks。
- 发布说明提交 `5c0303a` 的[远端 CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34049026354)：8/8 作业通过，含真实 API 容器和浏览器主流程；本次后续只更新运维事实记录。
- 实际发布提交 `f5b12b3` 的[远端 CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34051799735)：8/8 作业通过；浏览器 4/4（桌面 24.4 秒、手机 26.4 秒、资源 1.3 秒、自制混合作品 48.4 秒）。
- 权限后补 `a71e822` 的[远端 CI](https://github.com/hopeful220211/flightwoodx/actions/runs/34052626045)：8/8 作业通过，浏览器 4/4（1.7 分钟）；03:17 左右服务器已 fast-forward 并执行 dist 权限整理，919 个条目通过。
- 正式资源：94 个官方零件的 188 个模型/缩略图、35 张 WebP、1 张木纹、17 个 Blockly 素材、3 张旧作品封面，共 244 个；真实 HTTPS 字节范围读取、GLB/PNG/WebP 文件头检查成功，缺失模型/缩略图/优化图片/纹理/上传路径均返回 404。原一次脚本只遍历优化图片根目录，漏检子目录；已修正递归并重跑，不把此前的 191 项结果写成 226 项。
- 正式浏览器：独立验收账号注册成功；六件官方装配可见且保存；积木示例明确与账号同步；仿真停止/重置后重新运行，10.0 秒完成，终点 X=0、Y=0、Z=100。新无痕会话仅重新登录，从服务器恢复六件作品、名称及四步程序；积木 390×844、768×1024、1440×900 均实际查看。
- 03:20 API 实际重建，nginx 配置检查/reload 通过；前后 Mongo 容器 ID 与创建时间一致。正式 API 再次登录验证专用作品、零件、程序、跨账号拒绝及封面字节一致，资源 244 项再读通过。

### 正式服务器核验与发布准备（2026-09-07）

- 用户已登录阿里云，通过已有免密连接进入目标 ECS；`id` 返回 `uid=0(root)`，没有创建密钥、更换密码或放宽权限。GitHub 登录有效，前述交付分支可访问。
- `docker ps` 确认 nginx、API、MongoDB 三个容器运行；API 容器内部 `/api/health` 返回 HTTP 200、`status: OK`、`db: connected`，数据库 `ping` 返回 1。MongoDB 没有发布宿主机端口。
- `mongodump --version` 为 100.17.0，只证明备份工具可执行，不代表已备份或恢复演练通过。系统盘约 26 GiB 可用。
- 切换前生产 API 为 Node 20.20.2，使用 OSS；宿主机没有 Node。原目录 `/root/flightwoodx` 不是 Git 仓库，保持不变；新目录 `/root/flightwoodx-release-20260907` 通过 Node 22.23.2 容器构建。
- 切换前旧 API 没有挂载卷；只读遍历 `/app/apps/api/uploads` 得到 6 个文件、146,928 字节、0 个符号链接。已备份、填入持久卷并逐字节核对后才重建。MongoDB 使用现有命名卷，nginx 挂载前端目录和原证书。
- 只核验 OSS 与管理密钥是否存在，不输出值；使用现有凭据对固定官方模型执行 HTTPS HEAD，返回 403 AccessDenied。尚未验证 OSS 上传、历史用户文件或管理账号完整流程，也未修改存储桶权限。
- 后续已完成受限备份（约 434 MB）、全部压缩归档校验、独立 MongoDB 恢复及 20 个集合计数对账；恢复容器已清理，原生产卷未改动。
- 原 API 中 6 个上传文件已复制到新持久卷，逐文件比对一致（146,928 字节）。存量 Project 中 3 张封面引用 localhost，文件确实存在；已在发布时执行有精确计数、独占备份及条件更新保护的修复，正式 HTTPS 回读通过。
- 最终 `f5b12b3` 前端与 API 镜像构建成功，先检查新后端和 nginx，再只更新 API/nginx。已核对新 dist、原证书/ACME 目录和上传卷挂载，Compose 项目名保持 `flightwoodx`，Mongo 没有重建。
- 证书有效至 2026-11-30；原续期定时器和 webroot 保留，deploy hook 以 0755 安装并成功运行配置检查和 reload；未重新申请证书。
- 3 条已核实的 localhost 封面已条件修正，旧值备份在受限目录；生产 Project 缺少 `ownerId + forkFromPostId` 唯一约束，预检无重复后补建成功，没有删改重复数据。
- 原生产数据和登录凭据未删除、未重置，原目录保留。付费、删除正式数据、更换登录凭据等单独确认。

本机生产构建预览为 `http://127.0.0.1:4173`，仅当前电脑可直接访问；连接的是隔离临时数据库，不是正式账号/作品数据，不应作为评审正式数据的唯一保存位置。

## 4. 模块边界

| 模块 | 当前可用范围 | 仍未完成/不得承诺 |
| --- | --- | --- |
| 官网展示 | 图片、视频、现有导航与响应式界面本地验证 | 奖项/案例/实飞/供应承诺仍需用户提供事实证据；联系方式不能杜撰；页脚未建页面明确不可用 |
| 自制零件 | 手绘闭合、挤出预览、保存、列表、删除；混合自由作品、位置、账号恢复、来源错误处理 | 仅未连接摆放；卡扣、历史版本几何、板材标定、制造完整校验未完成 |
| 官方零件拼装 | 6 件主流程及双主板/父子几何回归 | 4–8 个起落架与旧飞行检查恰好 4 个的规则仍需负责人结合硬件冻结；几何对齐不是结构/实飞证明 |
| Blockly 与仿真 | 作品绑定、示例、编辑、保存恢复、运行/停止/重置 | 视觉流程不是物理仿真或真实飞行；AR 相机依赖设备权限 |
| 作品管理 | 云端保存/刷新/新设备恢复、重命名、删除、封面、发布 | 跨标签页/跨设备同时编辑仍为最后写入生效，未完成版本冲突协议；Project 兼容层尚未退出 |
| 社区与赛事 | 社区浏览器主流程与赛事持久化/权限 API 集成通过 | 赛事全状态机、审核运营、真实评分、未成年人合规未全部验收 |
| 用户与后台 | 注册登录、资料、角色鉴权、只读用户管理、真实只读审计分页 | 课程、零件审核仍占位；成员/学生全流程不是已完成功能；奖项和学习统计缺完整业务数据来源 |
| 导出 | 已有合法轮廓导出与授权/目录安全测试保留 | 官方零件缺获批二维制造源，备用 CAD 可能只输出缺失清单；不称完整可加工包 |
| 部署运维 | 正式发布、资源修复、备份恢复、旧上传复制及 API 重建持久化已验证；9 月 8 日新增受限前端自动发布及真实回退恢复 | 后端/数据库/部署器升级另行授权；全业务验收、定期异机备份、监控告警、历史凭据轮换未闭环；OSS 不可用，限流仍单进程内存实现 |

## 5. 当前待办与用户输入

1. 日常前端修改已可经 GitHub 自动发布，无需每次登录 Workbench。首次安装、专用权限、首次发布及受控回退恢复已通过；本轮无需用户继续操作服务器。凭据轮换、主机身份变化、磁盘不足或回退失败仍需单独处理，不能承诺永远免维护。
2. 中文命名、纹理、封面、工作台路由、异常保留草稿及三奖更正均已上线。用户已同意在正式业务验收时亲自登录网站管理员；不得把历史登录窗口或已登录回复当作完整管理员流程已经验收。除本次明确批准的奖项更正外，首页主要文字保持。
3. 正式管理员完整流程、社区写入、自制绘图的正式浏览器全流程尚未闭环；本地完整浏览器证据与正式 API 证据仍分开列示。
4. 用户只需处理无法代办的身份验证、付费或高风险操作确认及最终验收；若评审涉及硬件、制造、实飞或宣传事实，还需提供相应证据。保留页面文案不意味着已确认这些承诺。

具体执行与回滚见 [deploy/README.md](deploy/README.md)，本轮计划见 [评审前修复记录](docs/exec-plans/active/2026-09-07-review-readiness.md)。

## 6. 接下来怎么开发

本轮首页荣誉替换在本机 `/Users/nesty/Projects/flightwoodx-review` 的 `codex/home-hero-honors-2026-09-13` 分支，尚未推送，不能从下述历史远端分支取得这次新改动。

换电脑接续代码和本轮最新收尾文档时，使用 GitHub 的 `codex/automated-web-release-2026-09-08` 分支，再创建自己的 `codex/` 开发分支。正式前端源码对应受保护的 `production`（本次为 `77e4a19`）；最新收尾文档提交保留在开发分支，没有为文档再次切换网站。本机工作树为 `/Users/nesty/Projects/flightwoodx-review`。本轮没有把改动合并到原先存在未提交工作的 `chore/add-speckit` 工作树，默认 `main` 也不代表本次线上版本。

以后要求发布前端时，先确认当前提交通过全部检查，再按 [自动发布说明](deploy/automation/README.md) 快进 `production`，等待发布和公网验收。另一台电脑只需正常访问 GitHub，不必复制此次本机专用私钥；生产密钥由 GitHub 环境保管。涉及后端、数据库或部署器自身的改动不能用此静态发布流程代替。

完成剩余正式验收，准备实机、网络与账号环境。自制件自由摆放与账号恢复已接通，后续处理经过批准的连接设计、历史版本、制造源和规则依据。

后台课程/审核/学生工作流、作品多设备冲突、Project 迁移是明确后续开发项，不与已经验证的核心演示流程混为完成状态。所有新增能力继续遵守 `AGENTS.md` 的规格、测试、真实验收与证据更新要求。
